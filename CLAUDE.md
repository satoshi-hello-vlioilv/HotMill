# このリポジトリでの進め方

## 1. 会話が圧縮されたときは «要約» を信用しない

長い会話は途中で自動的に要約（圧縮）される。要約は情報が落ちるので、**要約を根拠に
作業を進めてはいけない**。次のことを必ず守る。

- **依頼の内容は、要約ではなく «依頼の原文» を読み直す。** 原文は会話ログに残っている
  （`/root/.claude/projects/.../*.jsonl`）。数値・条件・言い回しは原文が真実。
- **コードの状態は、要約ではなく «実ファイル» を読み直す。** `index.html` と `tools/*.mjs`
  を実際に開いて確認する。要約にある「実装済み」「修正済み」は当てにしない。
- **判断の根拠は、要約ではなく «評価器の実測値» で取り直す。** 迷ったら測る。

要約に書かれていることと実ファイルが食い違ったら、**実ファイルが正しい**。

## 2. 非同期で進める —— 確認待ちで止まらない

依頼者は離席していることが多く、やり取りは非同期になる。**確認が要る点で作業を止めない**。

- 確認が要る点は **README の残件表**（「残件（精度向上に必要なデータ）」）と、関係する
  コードのコメントに **問い合わせとして書き残す**。何が分からないのか・どちらの読み方が
  あり得るのか・どう仮置きしたのかまで書く。
- そのうえで **仮置きして先へ進める**。仮置きした値は 1 か所（`CONFIG`）に置き、
  回答が来たら 1 行の変更で差し替えられる形にする。
- 進められる作業を優先する。確認待ちの項目は最後に回す。

## 3. 直す前に測る

不具合の指摘を受けたら、まず «いまどうなっているか» を数値で出してから直す。
直したあとも同じ測り方で確かめる。その測り方は `tools/*.mjs` の評価器として残す
（一度見つけた不具合が二度と戻らないようにするため）。

判定の帯（しきい値）に実測の裏づけが無いときは、合否に数えず «参考» として毎回表示する
（`tools/calib.mjs` の目安時間、`tools/curltrace.mjs` の反り量がその例）。

## 4. 数値の出どころを 1 か所にする

同じ量が 2 か所に書かれていると、片方だけ直して食い違う。設備の寸法・能力は
`CONFIG` に 1 つだけ置き、描画も物理も検査もそこを見る。仕様書からいただいた値は
**そのまま残す**（見せ方を変えたいときは «倍率» を別に持つ。`CONFIG.CRANE_SPEED_K` がその例）。

## 5. 分からないことは «分からない» と言う

推測で埋めない。実機データが無い係数は、その旨をコメントと README の残件表に書く。
指摘が正しければ «どこが・なぜ・何がどう変わったか» を明示して直す。
指摘が食い違うと思うときは、根拠（実測値・図面・文献）を示して説明する。
