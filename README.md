# HotMill — 熱間圧延機シミュレータ

4段可逆式（4-High Reversing）アルミニウム熱間圧延機を、設備構造・圧延理論・操作系まで含めて
ブラウザ上で再現します。`index.html` を開くだけで動きます
（three.js と Font Awesome だけ CDN から読むので、初回はネットワーク接続が必要です）。

| どこを見るか | 中身 |
|---|---|
| **`index.html`** | アプリ本体。設備・物理・UI がすべてここに入っています |
| **`tools/*.mjs`** | 評価器。Playwright でアプリを起動し、実体の座標と物理量を測ります |
| **`docs/*.md`** | 資料。数字は評価器の出力をそのまま使い、推論では書きません |
| **`README.md`**（この文書） | **これから何をするか**（下の 0 節）と、設備・モデル・構造の説明 |
| **画面の `VER.x.y.z` バッジ** | **変更履歴。「どの版で・何が・どこに入ったか」はここが唯一の出どころです** |

---

## 0. これから —— 残件とロードマップ

**この節だけが «これからの話» です。**以降（1 節以降）は、いまどうなっているかの説明です。

### 0-1. いま最も効く 3 つ

どれも **«データをいただければ、推定を実測に置き換えられる»** ものです。
いずれも「何が分かれば何が決まるか」まで詰めてあるので、**回答が来たら 1 か所の変更で入ります**。

| # | 欲しいもの | それで何が決まるか |
|---|---|---|
| **1** | **実機のクロップ長**（頭・尻それぞれ何 mm 切っているか） | ワニ口の張り出しが先端で 922 mm、クロップ要求が 1,002 mm と**長すぎます**（実機の熱間可逆ミルは 300〜600 mm 程度が一般的）。`SLAB.OVERHANG` の 3 係数を合わせられます。**いまは推論で動かしていません** |
| **2** | **偏芯補償の方式と効き**（実機の AGC が持つノッチフィルタ相当） | **未実装**です。実機のミル定数を入れてから長手の板厚の振れが **3σ 平均 45 → 56 µm** に上がり、いただいた実力 **±50 µm** より 12 % 高くなりました。外乱を 1 つずつ切って測った結果、**偏芯を切れば 3σ 44 µm でちょうど実力に入ります** —— 差の説明として残っているのはこれだけです（詳細は 0-4） |
| **3** | **同じ材質でパス間時間を変えたときの荷重実績 2 点** | 組織の履歴（亜結晶粒径・再結晶率・**残留ひずみ**）は状態量として持っていますが、**まだ荷重へ返していません**。待ち時間が長い ＝ 再結晶が進む ＝ 荷重が下がる、の差が取れれば係数を同定できます。**8 割できていて、残るのは返す 1 本の式とこの 2 点だけ**です |

### 0-2. モデルの係数 —— 実機データがあれば推定を置き換えられるもの

| # | 欲しいデータ | いま何で代用しているか |
|---|---|---|
| 4 | **クーラントの沸騰曲線**（濃度・劣化度ごとの熱流束 vs 板面温度、ライデンフロスト点） | 一般的な傾向値（`COOLANT.CURVE`）。濃度 1 % で核沸騰 5.5 %・ライデン 7 K、劣化 0 → 1 で核沸騰 30 %・ライデン 25 K |
| 5 | **上面ヘッダ／下面スプレーの実流量** [L/min] と濡らす面積 | 上面 1.2・下面 1.4 kg/m²s（`SPRAY.*.FLUX`）。**上がり温度にはほとんど効きません**（実測: 下面 1.4 → 0.7 で 1 K、上面 1.2 → 0.6 で 3 K）。上がり温度を決めているのはロール抜熱（`MATERIAL.H_ROLL`。25,000 → 12,000 で 16 K）で、**実機の上がり温度 360 ℃ に合わせ込んでいるのはそちら**です。液流束の実測がいただければ、合わせ込みの残りをロール抜熱の側から外せます（`tools/coolfit.mjs`） |
| 6 | **クーラント劣化度の定義**（更新周期・トランプオイル量・粒径など） | 0（新液）〜1（更新時期）の 1 つのつまみ。既定 0.35 |
| 7 | **ピット炉の均熱条件**（保持時間）と抽出時の上下温度差の実測 | 50 K（`FURNACE.SOAK_DT`）。ご提供の見立て |
| 8 | **抽出から持ちかかりまでの実時間** | 10 分（`SUPPLY.HOLD_MIN`）。ご提供の見立て |
| 9 | **上下ロールの径差と、そのときの反りの実測**（＋ミル出側での先端の浮き上がり量） | `DIA_DIFF.CURL_K = 1.0`（周速差のうち板の伸びの差として残る割合）。第一近似で、反りの «量» を裏づける実測がありません |
| 10 | **各材質の荷重実績**（A5052 / A5083 / A6061 / A7075 ほか） | 構成式は Sellars–Tegart の物理量で持ち、**活性化エネルギー Q と応力指数 n・α は公表値そのもの**です（`modeltrace` が式から逆算して一致を確認）。残るのは**絶対値を決める `LN_A` だけ**で、これは荷重実績でしか較正できません。いまは A1100 の実機ロットに合わせた値からの推定です |
| 11 | **組織モデルの係数** | 亜結晶粒径 `1/d ＝ A ＋ B·log₁₀Z`（A −1.00・B 0.100）と静的再結晶の Avrami `t₅₀ ＝ A·ε⁻²·exp(Q/RT)`（A 1.77×10⁻¹⁴・Q 180 kJ/mol・N 1.4）は、**アルミ熱間加工の一般的な形と桁で置いた推定値**です。出る値は実測レンジ（亜結晶粒径 1〜10 µm）に入っていますが、**この材料・この圧延機で測った値ではありません**。圧延後のコイルから亜結晶粒径（TEM / EBSD）か硬さが 1 点でも取れれば較正できます（`CONFIG.MICRO`） |
| 12 | **主電動機の GD²（回転子慣性）** | **トルクカーブは実機値に置き換え済み**（最大 1,520 kN·m・ベース速度 75 mpm・定格 1,400 kN·m・最高速度 160 mpm・過負荷耐量 225 %/60 s）。**残るのは回転子慣性だけ**で、電動機の外形（Φ2,000 × 4,600L）から「回転子は外形の 0.60 倍の径・0.45 倍の長さの中実円柱、等価密度 7,000 kg/m³」と置いた推定値 **2.9 t·m²/台**（ロール軸換算 209.6 t·m²）です。銘板の GD² が分かれば置き換えます（`DRIVE.ROTOR_*`） |
| 13 | **ロール軸受の摩擦係数** | 油膜軸受（モーゴイル）の文献値の中央 **0.003**（`DRIVE.MU_BEARING`）。文献の幅は 0.002〜0.005 で、圧延トルクに対する損失はこの幅で **1.8〜4.5 %** に振れます。実機で「無負荷（空転）で主機が食う動力」が分かれば直に決められます |
| 14 | **ミル定数の 5 mm より先** | 測定値は伸び 0〜5 mm（荷重 0〜1,314 t）まで。本アプリの荷重は最大 3,200 t（伸び 11.4 mm）まで行くので、**5 mm より先は最後の増分 296 t/mm のまま外挿**しています。硬くなり続けると置くのは根拠が無いため、保守側（やわらかい側）に振ってあります。高荷重側の測定が伸びれば `MILL.MODULUS_TABLE` に足すだけです |
| 15 | **サイドガイドのコロ径・シャフト径** | ピッチ 350 mm と入側の並びは図面どおり入っています。**残るのはコロ径とシャフト径**（Φ130 / Φ45 の推定値）です |
| 25 | **停止時間と偏芯の関係の実測**（「何分止めたら偏芯がいくつ出たか」） | 熱偏芯は «バイトに居る角度だけが温まる» 形で入れました（`GAUGE_MODEL.ECC_TH`）。到達する半振幅 20 µm は «線膨張 12×10⁻⁶/K × 実効の温度差 70 K × 染み込み深さ 25 mm» の見当で、時定数（温まり 45 s ／ 均され 150 s）も推定値です。**実機で «何分止めたら偏芯がいくつ» が 1 点でも取れれば較正できます**（`tools/ecctrace.mjs`） |
| 16 | **ワニ口のあごの «開きが板厚の何割か»** | いただいた写真は真横からでないため、**比を決められませんでした**（推論で埋めていません）。板厚で頭打ち（口 ≤ 板厚の半分）にはしてあります。真横から、スケールになる物を添えた 1 枚があれば較正できます |

### 0-3. 約束ごと —— 決めていただければ実装できるもの

| # | 決めていただきたいこと | いまどうしているか |
|---|---|---|
| 17 | **熱膨張の「冷間／熱間」の約束**（入力寸法はどちらか、板厚計の表示はどちらか） | **未実装**（線膨張係数も材質表にありません）。影響は小さくなく、433 ℃ では厚み 530 → 535.5 mm・幅 1,330 → 1,344 mm・長さ 3,450 → 3,486 mm。とくに効くのは出側板厚で、360 ℃ の 8.00 mm は室温で **7.93 mm（−68 µm）** ―― 較正した ±50 µm の板厚精度より大きい量です。**推奨: 入力寸法＝冷間／内部＝熱間／目標板厚と板厚計の表示＝冷間換算を主値・熱間実測を併記** |
| 18 | **上がり温度の測定位置**（板の上面か、板厚方向の平均か） | 板厚方向の平均で比較しています。上下で冷却を分けたので、上面測定なら比較の基準が変わります |
| 19 | **PIT 炉の «2 本の合計幅» のしきい値を炉別にするか** | いまは全炉で 2,660 mm（実機の運用値）。この値が «炉の内法 − トングの作業代» の計算値なら、内法 W 4,300 の 7〜8 号では 2,200 mm になるはずです。炉別にするなら 1 行の変更で切り替えられます（`FURNACE.PAIR_W`） |
| 20 | **A1100 ロットの上がり温度と圧延後の板長** | 未提供。数字は出しますが**合否には数えていません**（本アプリ 353 ℃ / 299 m） |
| 21 | **長尺材（板長 4,200 mm）をどの炉へ入れるか** | ピット炉は深さが 2 通り（1〜6・9 号 4,050 mm ／ 7・8 号 4,800 mm）で、スラブは**立てて**入れます。入力できる最大の板長 4,200 mm は浅いほうの炉には立ちません（150 mm 足りない）。実機で長尺材を 7・8 号に限っているのか、寝かせて入れるのかが分からないので、**合否には数えず参考として毎回数値を出しています**（`tools/eval.mjs`） |
| 22 | **2nd リミットで «維持する» MUV が何を指すか** | 圧下装置の仕様に「2nd リミット 4,200 ton ⇒ 油圧シリンダ急速開、ミル急停止（テンション、MUV は維持）」とあります。テンション（巻取張力）は分かるのですが、**MUV が何の略か分かりません**（推測で埋めていません）。いまは «張力を落とさない» ことだけ実装し、MUV は触っていません |
| 23 | **較正の基準ロット（A5052・持ちかかり 433 ℃）はどの操業パターンか** | ソーキングマスタを入れて分かったことです。A5052 の社内記号は 52S で、表では **A 操業**（均熱 480 ℃・保持 1 h 下限）に載っています。ところが抽出（480 ℃）から持ちかかりまでの 10 分では、放射と自然対流では **3 K 程度しか下がらない**計算で、**実測の 433 ℃（−47 K）が説明できません**。基準ロットが A 操業だったのか、抽出から持ちかかりまでの実時間が 10 分より長いのか、持ちかかり温度の測り方（表面／板厚平均）が違うのか —— **どれなのかが分かると、持ちかかり温度をソーキング条件から出せるようになります**（いまは入力値をそのまま使っています） |
| 24 | **社内材質記号と JIS 記号の対応** | 実機の操業基準は社内記号（52S・3S・75S・61S …）で書かれているので、本アプリの JIS 記号（A5052 …）と橋渡しが要ります。いま引けているのは **A5052→52S / A3003→3S / A7075→75S / A6061→61S / A4045→4045 の 5 つだけ**で、A1050・A1100・A5083 は表に代表材が無く**推測で埋めていません**（`ALLOYS[].soak` が null）。**社内材質の一覧**をいただければ材質マスタとして入れられます |

### 0-4. 未解決 —— 原因が特定できていないこと

| # | 事象 | どこまで分かっているか |
|---|---|---|
| 21 | **素材長によっては圧延後の体積が «作られる»** | 体積保存の法は **«板 ＋ 切り落とした材料 ＝ 素材»** です（板だけの増減はクロップで落ちたぶんなので不具合ではありません）。そちらで測ると、**出荷ロット 3,450 は +0.8 %・2,600 は +1.1 %・2,400 は −0.3 % で正常**。**素材長 3,000 で +24.3 %、2,000 で +132.2 %** だけが本物の不具合です。**分かれるのは «16 mm へ入るパス» の 1 本**で、その途中で板の平均板厚 `hEff`（＝ 材料体積 ÷ 板長）が出側ギャップ 16.1 mm を割り込んで 12.41 mm まで落ちます —— パス途中の板は «まだ 26 mm の部分» と «もう 16 mm の部分» でできているので**物理的に起こりません**。材料体積 `_vol` は保たれているので、走っているのは**幾何の側**（`length ＝ xMax − xMin` が材料より速く伸びる）。**原因は未特定**。測り方は `tools/voltrace.mjs`（`--only=3450` で 1 つだけ速く回せます）。**前の版 `fb460f1` でも同じ数字が出ます**（今回の変更が原因ではありません） |
| 22 | **長手の板厚の振れが実力より 12 % 高い** | 実機のミル定数（226〜296 t/mm、従来置いていた 550 t/mm の半分以下）を入れてから 3σ 平均 45 → 56 µm。**つまみを 1 つずつ切って測りました** —— 摩擦の揺らぎは切っても変わらず（19.2 → 19.2 µm）、スタンドの減衰を上げても下がらず（ζ 0.35 → 0.9 で 18.7 → 20.6 µm）、**偏芯を切ると −23 %**（18.7 → 14.6 µm）、残りは**前のパスの凹凸の持ち回り**（δ·Q/(M+Q)。ミルが柔らかいほど大きい）。**動かせるつまみはもうありません** —— 偏芯の振幅 `ECC_BR` 0.012 mm（半振幅＝全振れ 24 µm）は実機の BUR の TIR 30〜80 µm の**下限以下**、持ち回りは実機のミル定数から物理的に決まる量です。偏芯補償は**実装しました**が、いまの整定では雑音のほうが大きく効きません（→ 0-4 の 24）。偏芯を切ったときの σ 14.6 µm ＝ 3σ 44 µm はちょうど実力に入るので、**説明は «偏芯を実機なみに打ち消せていない» ことのまま**です |
| 23 | **巻取パスでスタンドが 15 Hz で鳴く** | **不具合ではなく、実在の現象の再現**です。スタンドのばね M と材料のばね Q は連成し、実効減衰は `ζ/√(1+Q/M)` に下がります —— ミルが柔らかいほど鳴きやすく、板が薄く Q が最大の巻取パスで出ます。実測でギャップの自己相関の山が**ちょうど 15.00 Hz**（`CONFIG.MILL.STAND.FN`）に立ちます。圧下（油圧・周波数応答 5 Hz）はこの速さでは追えず、**制御は発振していません**。実機の «チャタ（第 3 オクターブ振動）» に相当します。`tools/loadtrace.mjs` が «制御帯域の往復»（合否）と «固有振動»（参考）を分けて出します |
| 24 | **偏芯補償の整定** | 仕組みは入っています（荷重の «ロール回転に同期した成分» を同期検波で取り出し、圧下の応答遅れを先回りして逆位相を足す）。ところが**いまの整定では、打ち消せる偏芯より推定の雑音のほうが大きく、入れると板厚の振れが増えます** —— 長手の板厚の σ 平均で 補償なし 19.5 µm ／ あり 23.6 µm ／ あり＋荷重から抜く（NOTCH）138.8 µm。偏芯そのものが σ に効いているのは 15 % 程度（23.6 → 19.9）。原因は «噛んでいるあいだの荷重にスタンドの鳴き（15 Hz）と AGC 自身の動きが乗っていて、ロール回転（0.3〜1 Hz）の成分の SN 比が足りない» ことまでは分かっていますが、**整定は未解決**です。そのため**既定は «切»** にしてあり、画面の «運転モード» から入切できます（`tools/ecctrace.mjs` / `tools/gaugetrace.mjs` が毎回数値を出します） |
### 0-5. 実装ロードマップ

**荷重まわり**は `docs/圧延荷重に効く要因.md` の 6 節にまとめています ——
«入れれば効く順» ではなく **«入れないと説明できない現象がある順»** に並べ、
必要な実機データを項目ごとに添えてあります。

| 優先 | 項目 | 見込み |
|---|---|---|
| ★★★ | **残留ひずみを変形抵抗へ返す** | 状態量は持っているのに荷重へ返していない。**8 割できています**（→ 0-1 の 3） |
| ★★★ | **接触弧内の温度降下** | ひずみ速度は分布させたが温度は一定のまま。表層は冷え芯は温まる |
| ★★★ | **混合潤滑（μ を «決まる量» にする）** | いま最も根拠の薄い量（経験式）が、薄板パスで最も効く要因になっている |
| ★★ | ひずみ依存（ピーク応力と定常応力）／弾性入口・出口域／ロール偏心／幅広がり | — |

**形状・板厚・制御まわり**は下の 2-3 節にあります。

---

## 0-6. 版数（バージョン）の管理と変更履歴

画面左上、製品名の横に **`VER.x.y.z`** のバッジが出ます。押すか <kbd>V</kbd> で
**変更履歴ビュワー**が開きます。「絞る → 選ぶ → 読む」の 3 段です。

| 場所 | できること |
|---|---|
| 上・検索欄（<kbd>/</kbd> で移動） | 変更点の本文と `w`（どこに入ったか）の両方に語を当て、当たった所を目印で囲む |
| 上・分類ボタン | **物理・制御／設備値・材料／3D・部品／画面・操作／評価器／資料** の 6 つで絞る。件数が先に出るので、押す前に結果の量が分かる |
| 左・版の並び | 版数・日付・表題・件数と、**分類の帯**（この版が何を触った版かが色の比で分かる）。<kbd>↑</kbd><kbd>↓</kbd> で移れる |
| 右・版の中身 | 選んだ版の変更点を分類ごとにまとめて表示。`git log base..次の版の base` をその場で複製できる |

分類は `w` から機械で決めます（`VERSION.KINDS`）。項目ごとに手で持たせると足すたびに
付け忘れるためで、色は分類と 1 対 1（`#dlg-ver` の `--k-*`）です。

| 桁 | 上げる条件 |
|---|---|
| **x** | 機構そのものの作り直し、座標系や設計の前提が変わる改修（`1.0.0` = 版数管理の起点） |
| **y** | 設備や機能の追加、図面の反映 |
| **z** | 不具合修正・数値の調整だけの更新 |

**更新の手順は 1 つだけです。** `index.html` の 0 節にある `VERSION.LOG` の**先頭に 1 件足す**
（`v` / `base` / `date` / `title` / `items`）。`items` の `w` には «どこで実装したか» ＝
主な関数・データ名を書きます。

`base` は **その版の作業を始めたコミット**（＝ 1 つ前の版の最後のコミット）です。
書く時点で必ず分かる値なので、あとから前の版へ書き足す必要がありません。
これで、画面の版数からその版の差分まで直接降りられます。

```bash
git log 0704131..33b11db   # VER.0.8.0 に入ったコミット（base .. 次の版の base）
git log 33b11db..HEAD      # 最新の版に入ったコミット
git diff 33b11db..HEAD     # 最新の版で何が変わったか
```

版数・日付・履歴の**真実は `VERSION` の 1 か所だけ**で、バッジ・履歴パネル・ブラウザのタブ名・
評価器はすべてそこを見ます。したがって「画面には新しい版が出ているのに中身が古い」という
食い違いが起きません。`tools/eval.mjs` が、バッジの文字列と `VERSION.label` の一致・履歴の並び・
`base` の重複が無いこと・**最新の版に変更点が溜まりすぎていないこと**（過去の版の中央値の 3 倍以内）・
分類が全項目に付いていることを検査します。最後の 2 つは、1 つの版に変更を積み続けて
「画面の版数が動かないまま中身だけが変わる」のを防ぐためのものです（実際にそうなったことがあります）。

## 1. 対象設備の諸元

| 項目 | 値 | 出典 |
|---|---|---|
| ワークロール | Φ930 × 2500 mm（最小径 Φ885）／ 関東特殊鋼 **7 組** | 実機仕様 |
| WR 硬度 / 粗度 / クラウン | 60〜70 HS / 0.60〜1.10 μmRa / **+150 μm**（径差・中凸） | 実機仕様 |
| バックアップロール | Φ1430 × 2450 mm（最小径 Φ1350）／ 日本製鋼 **2 組** | 実機仕様 |
| BUR 硬度 / 粗度 / クラウン | 45〜50 HS / 0.60〜0.20 μmRa / **−50 μm**（径差・中凹） | 実機仕様 |
| 主電動機 | **2,000 kW × 2 台**（更新後）、0 / 150 / 330 rpm、減速比 5.96:1 | 実機仕様 |
| 主機のトルクカーブ | **最大 1,520 kN·m**（ベース速度 75 mpm まで一定）／ **定格 1,400 kN·m** ／ 最高速度 160 mpm ／ **過負荷耐量 225 %・60 s** | 実機の実測カーブ |
| ドライブ・受電 | **IEGT インバータ** ／ トランス **2,800 kVA × 2 台** ／ 高圧遮断器 **真空遮断器（VCB）** | 実機仕様 |
| 最大圧延速度 | **160 mpm**（トルクカーブの最高速度） | 実機仕様 |
| ミル定数 | **一定ではない**。伸びの帯ごとの増分ばね定数 0〜1 mm **226** ／ 1〜2 **244** ／ 2〜3 **267** ／ 3〜4 **281** ／ 4〜5 **296** t/mm | 実機の測定値 |
| 常用最大圧延荷重 | 3,920 ton（圧下スクリューナット収納部） | 実機仕様 |
| 荷重リミット | **1st 3,900 ton → 荷重一定制御** ／ **2nd 4,200 ton → 油圧シリンダ急速開・ミル急停止**（テンション、MUV は維持） | 実機仕様 |
| **電動圧下装置** | 2 速切換式。高速 1 台 110/220 kW D.C.（0/500/1000/1250 rpm・減速比 27.5:1）／ 低速 2 台 52 kW D.C.（0/550 rpm・440:1） | 実機仕様 |
| 電動圧下 圧下速度 | 高速 **0 / 8.5 / 17 / 21.2 mm/s** ／ 低速 **0 / 583 µm/s** | 実機仕様 |
| 手動レバー | 同時圧下 1 ノッチ 0.1 mm/s ／ 2 ノッチ 1 mm/s ／ レベリング 0.1 mm/s・side（O.S. と D.S. の差では 0.2 mm/s） | 実機仕様 |
| **油圧圧下** | シリンダ径 **Φ1,060**、圧力 **210 kgf/cm²**（吐出 250・背圧 40）、ストローク max 60 mm（有効 52 mm） | 実機仕様 |
| 油圧圧下 速度 / 応答 | 約 **3 mm/s**（荷重 1,500 ton 時）／ 周波数応答 **5 Hz（±50 µm）**（1,500 ton・CPC モード・**90° 位相遅れ**） | 実機仕様 |
| 入側テーブル長 | 121.8 m | 実機仕様 |
| 出側テーブル長 | 80.1 m | 実機仕様 |
| テーブルローラ | 胴長 2,490 mm、胴端 Φ281.6 → 中央 Φ219.6（勾配 1:25）、軸全長 3,438 mm | 図面 |
| テーブル区分 | 入側 D / C-2 / C-1 / B-1 / A-9〜A-1、出側 F / G-1〜G-3 / H-1 / H-2 / J-1〜J-3（本数・ピッチは図面） | 図面 |
| 30 mm シャー / パイラー | J-3 の先（80.9 m）／ 82.3〜92.3 m、定尺 9 m | 実機（位置は推定） |
| サイドトリマー | ミル中心から 5,875 mm | 図面 |
| スリーロール | ミル中心から 6,900 mm（下2本・上1本） | 図面 |
| 巻取リール | ミル中心から 8,350 mm、パスライン上 1,200 mm | 図面 |
| 3ロール構成 | 下 2 本 Φ230（軸間 260 mm）＋ 上 1 本 Φ320 | 図面 |
| このラインでの最大コイル径 | Φ1,510（8 mm × 56 巻 / 10 mm × 45 巻） | 図面 |
| 75 mm クロップシャー | ワークロール出側から約 30.6 m ／ **上刃固定・下台上昇式・押え無し** | 実機（精度低） |
| 75 mm シャー 最大剪断力 | 368 ton | 実機仕様 |
| 75 mm シャー 刃 | 75 × 180 × 2500 mm、材質 NS3C、硬度 Hs 65±3° | 実機仕様 |
| 75 mm シャー 1 カット | 300〜500 mm（総切断長は板厚 60 mm 換算で 300〜2,400 mm） | 実機運用 |
| 材質 | A1050 / A3003 / A4045 / A5052 / A5083 / A6061 / A7075（JIS 代表組成） | 公表データからの代表値 |
| パスライン高さ | 900 mm | **推定値** |

### 出側仕上げ設備の諸元

| 項目 | 値 | 出典 |
|---|---|---|
| **サイドトリマー** 型式 | スタータイプ・スクラップチョッパ付きクイックオープン型 | 実機仕様 |
| 刃物 | Φ510〜498、厚 40〜30 mm | 実機仕様 |
| 被せん断材 巾 | トリム前 2,350 mm / トリム後 760〜2,300 mm | 実機仕様 |
| 被せん断材 厚 | 7.0 mm 連続 / 9.5 mm 非常最大 | 実機仕様 |
| 電動機 | 110 kW × 1 台（直流）、0 / 500 / 600 rpm、0〜208 mpm | 実機仕様 |
| **巻取機** 型式 | 4セグメント・コラプシブルウェッジ式（アウトボードベアリング付） | 実機仕様 |
| 巻取コイル寸法 | 巾 760〜2,100 mm、外径 Φ760〜Φ1,900、内径 Φ610 | 実機仕様 |
| トリマー刃 開位置 | 軸心がパスライン ±305 mm（開口 100 mm） | 図面 |
| コイル重量 | 最大 9,000 kg | 実機仕様 |
| 巻取テンション | 最大 14,480 kg（設定変更で 20,500 kg） | 実機仕様 |
| 電動機 | 550 kW × 1 台（直流）、0 / 300 / 900 rpm、減速比 8.5:1、0〜185 mpm | 実機仕様 |
| 板厚（コイル） | 6.0〜10.0 mm | 実機仕様 |

### 主機の包絡線 —— トルクカーブは «実測» で持つ

主機の能力は**実機のトルクカーブそのもの**を持ちます（推定で組み立てません）。

```
   トルク
   1,520 ┤━━━━━━━━┓                 最大 1,520 kN·m（ベース速度 75 mpm まで一定）
   1,400 ┤        ┃╲                定格 1,400 kN·m
         │        ┃  ╲___           そこから上は定出力（T = T_max·v_B/v）
         └────────┸──────┴──→ 速度
                 75            160 mpm
```

**検算**: 1,520 kN·m × 2.688 rad/s ＝ **4.09 MW** で、定格出力 4.0 MW（2,000 kW × 2 台）と合います。
過負荷耐量は **225 % ・60 s** で、スケジュール生成の動力上限もこれをそのまま使います
（以前置いていた推定の margin は廃止しました）。

速度と径の関係は残ります —— ベース速度も最高速度も**ロール径に比例**するので、
研削して細くなれば下がります（`Drive.baseSpeed` / `maxSpeed`）。
所要トルクが大きいパスほど定出力域で出せる速度が下がり、運転時のリミッタも
この包絡線で動くため、圧延荷重が上がると自動的に減速します。

### テーブル長が非対称な理由

**入側 121.8 m は「巻取直前のパスで板が載りきる長さ」そのものです。**
板は圧延で `L₀·h₀/h` まで伸びるので、**巻取パス以外は入側／出側テーブルに載り切る必要があります**。

既定ロット（530 mm × 3,450 mm）なら、巻取直前の 16 mm で **114.3 m**（＝ 3,450 × 530 ÷ 16）——
入側 121.8 m にちょうど収まります。そこから最終 1 パスで 8 mm（215 m）へ落として巻き取る、
という構成です。**つまり巻取リールは付加設備ではなく、この板厚を作るための必須設備**です。

逆に、同じ板厚で**最大長 4,200 mm のスラブは成立しません** —— 16 mm で 139 m になり、
入側テーブルから溢れます。既定の板長を 3,450 mm にしてあるのはこのためです
（`CONFIG.SLAB.LEN_DEFAULT` にその計算をコメントで残しています）。

### テーブルの区分とローラ配置（図面）

図面「各テーブルローラ本数」と区間長を `CONFIG.TABLE.SECTIONS` にそのまま持ち、`Layout` が配置します。

| 区分 | 範囲 [m] | 本数 | ピッチ [mm] | 配置 |
|---|---|---|---|---|
| E-1 / E-2 | 0.75–2.15（ミル直近） | 8 / 8 | 466（4 段 × OS/DS 2 本） | 図面どおり |
| D | 2.5–7.5 | 14 | 460 / 370×11 / 460 | 図面どおり |
| C-2 | 7.5–11.4 | 7 | 510×3 / 600×3 | 図面どおり |
| C-1 / B-1 | 11.4–18.6 / 18.6–25.8 | 12 / 12 | 600 | 図面どおり |
| A-9〜A-5 | 25.8–70.8 | 12 ×5 | 750 | 図面どおり |
| A-4 / A-3 | 70.8–84.3 / 84.3–97.8 | 18 / 18 | 750 | 図面どおり |
| A-2 / A-1 | 97.8–109.8 / 109.8–121.8 | 16 / 15 | 750 | 図面どおり（A-1 は余り 0.75 m をミル側へ寄せる） |
| F | 2.5–5.3 | 8 | 460 / 370×5 / 460 | 図面どおり |
| G-1 | 9.0–14.9 | 10 | 650 | 図面どおり |
| G-2 / G-3 | 14.9–22.3 / 22.3–29.8 | 10 / 10 | 750 | 図面どおり |
| 75 mm シャー | 29.8–31.4 | – | – | 下台 640 + 刃（下は端材ピット） |
| H-1 / H-2 | 31.4–38.9 / 38.9–47.1 | 11 / 11 | 750 | 図面どおり |
| J-1 / J-2 / J-3 | 47.1–56.1 / 56.1–66.6 / 66.6–80.1 | 12 / 14 / 18 | 750 | 図面どおり |

配置の規則は 1 つだけです: **ピッチの合計が区間長に収まればそのピッチで並べ**、余りが半ピッチを超える分だけ
ミル側へ寄せる。収まらなければ区間内に等配する（概算）。現状は全 24 区分が図面のピッチで並びます。
出側テーブルは**リールの手前（F の終わり 5.3 m）で終わり**、リール下（旧 «ﾃﾞﾌﾟﾚｯｼﾝｸﾞ» 2 本）は
コイルカーが入るため空けてあります。3ロール下の 2 本（軸間 260）はスリーロール自身の下ロールです。

区分の名前・本数・ピッチは、ツールバーの「テーブル記号」（<kbd>Z</kbd>）で 3D 上にバッジとして出せます。

**テーブル選択**（図面の 1: C-1〜G-1 / 2: B-1〜G-3 / 3: A-6〜H-2 / 4: A-1〜J-3）も持っています。
パスの開始時に «今の位置からパス終了時に板が止まる位置まで» を覆う最小の選択番号を
`Layout.select` が選び、選ばれた区間のローラだけが回ります（`MillController._selectTables`）。
状態バナーに「テーブル選択 n（区分）」として出ます。

### ロールとチョック（«チョックをはめた状態» の再現）

図面どおり、ロールは胴の外側で次のように段が付きます（`Parts.roll`、寸法は `CONFIG.MILL.NECK`）。

| 区間 | 径・長さ | 役割 |
|---|---|---|
| 胴 | WR Φ930 × 2,500 / BUR Φ1,430 × 2,450 | 圧延面 |
| 胴端の逃げ | 胴径 × 0.92 × 90 mm | 胴とフィレットの間の逃げ |
| フィレット | 胴径 × 0.92 → 首径、長さ 160 mm | 応力集中を避けるテーパ |
| 軸受首 | WR Φ520 / BUR Φ850、チョック外端（z = ±2,860）まで | チョックの軸受が抱く |
| 軸端 | 首径 × 0.72、長さ 340 mm（駆動側 320 mm） | 操作側は円筒で終わる |
| 二又（フォーク） | 背板 260 + 爪 460、すき間 = 首径 × 0.50、厚み = 首径 × 0.62 | **駆動側のワークロールだけ**。スピンドルの平頭を受ける |

チョック（`Parts.chock`）は上下 2 枚のキーパプレート（厚 90 mm・ボルト 4 本ずつ）で本体を挟み、
内部に軸受ボス、両側面に**軸受カバー（首径 × 1.35、中心ボルト付き）と給油ブロック**を持ちます。
ハウジングの窓（±800 mm）に接する面には**摺動ライナ**（厚 70 mm）が付き、位置は窓の寸法から
逆算するので、窓を変えてもライナがはみ出しません。BUR チョックには上下に**受け座**（高さ 180 mm）が
あり、上は圧下ロッド、下はパスライン調整ウェッジがここに当たります。

駆動側では、スピンドルの端にも**平頭（スペードブレード）**があり（`Parts.spindleHead`）、
自在継手の中心に置いてロールと同じ角度で回ります。圧下でロールが上下するとスピンドルは
振れますが、平頭の高さは二又のすき間より小さく取ってあるので、**圧下の全域で又に収まり、
又の底も突きません**（`tools/rolltrace.mjs` で全ギャップを実測）。

### ロール研削とパスライン調整装置

ロールは研削のたびに径が小さくなります（WR Φ930→Φ885、BUR Φ1430→Φ1350）。
下ワークロール上面をパスラインに保つには、減径ぶん（最大 62.5 mm）を下バックアップロール
チョックの下で持ち上げる必要があり、それがパスライン調整ウェッジの役割です。
サイドバーの「ロール研削状態」を動かすと、ロール径の変化とウェッジの厚み変化が連動します。

テーブルローラは胴端 Φ281.6 部で材料を支持し、中央へ向かう 1:25 のテーパで自動調心します。
したがってパスラインの基準は胴端半径（140.8 mm）です。

### 圧延機ピットと «地面の無い場所に立つもの» の排除

ピットは **OS/DS 方向は広く**（ロール交換に必要）、**長手方向はソールプレートが収まる最小**
（±2,300 mm）に絞っています。深さはソールプレート下面（−3,600 mm）に一致させ、ハウジングは
ソールプレートの上に、ソールプレートはピット床の上に載ります（以前はハウジングがソール
プレートを貫き、下端がピット床より 35 mm 下にありました）。

ピットの真上には何も立てません。そのために次を改めました。

| 以前 | 問題 | 現在 |
|---|---|---|
| テーブルローラが x = ±900 / ±1,800 から始まる | ±1,800 のローラの軸受と架台がハウジングの柱（x 800〜2,150・z 1,400〜2,700）の **中に埋まり**、架台はピット開口の真上に立っていた | ハウジング外面（±2,150）の外側、**±2,700 から** 始める。架台はピット壁の天端（x 2,300〜2,700）と床に載る |
| ミル直下の敷板が宙に浮いていた | 支持が無い | 窓の間の敷板はハウジング内面（z = ±1,400）に渡した **ブラケット梁** で受け、ハウジング外側の敷板は **ピット壁の上に立てた脚** で受ける |
| サイドガイド（x = 2,700 ± 1,800）がハウジングの柱を貫通し、架台が床から 380 mm 浮いていた | 構造不成立 | ハウジング外面の外（2,450〜4,350）へ移し、**テーブル架台の外側（z = ±2,300）に床から立つ固定架台** のアーム軸受を貫いて開閉する。装入スラブの移載位置（−6,500）とも重ならない |
| トリマー・シャーの基礎、減速機、主電動機（Φ2,900）、転倒装置の基部が床下に潜っていた | 軸心 1,083 mm より半径が大きい | すべて **床上の据付ベース（300 mm）** に載せ、減速機・電動機は Φ2,000 に改めた |
| 炉のピットが床版を貫いていた | 開口が無い | 床版は圧延機ピットとピット炉の **2 つの穴を持つ 1 枚の Shape** として作る |

これらは目視ではなく `tools/structure.mjs`（接地・ピット上の浮き・部材間の食い込み）で
機械的に判定しています（後述）。

### スラブ諸元

| 項目 | 値 |
|---|---|
| 呼び厚（鋳造厚） | 406 / 508 / 560 mm |
| 面削量 | 片面 8〜15 mm（両面）。圧延前の実厚 = 呼び厚 − 2×面削量 |
| 長さ | 最大 4,200 mm |
| 幅 | 900〜2,200 mm |

面削面（横置きしたときの上下面）は鏡面状に仕上がるため、鋳肌のままの側面・端面とは
別のマテリアルを割り当てています（BoxGeometry の +Y / −Y グループのみ鏡面）。

### 材質（JIS 代表合金）

材質は `CONFIG.ALLOYS` に 1000〜7000 系の代表組成を持ち、サイドバーで選べます。
材質を変えると、圧延荷重・パス数・温度推移・端部欠陥（クロップ長）・反りがすべて変わります。

変形抵抗は **Sellars–Tegart（双曲線正弦則）**で持ちます。

```
      Z = ε̇·exp(Q/RT) = A·[sinh(α·σ)]ⁿ        →  σ = (1/α)·asinh[(Z/A)^(1/n)]
```

**4 つの材料定数は役割が分かれています。**

| 定数 | 何を決めるか | 出どころ |
|---|---|---|
| `Q_ACT` | **温度依存だけ**（活性化エネルギー [kJ/mol]） | 公表値そのまま |
| `N_EXP`・`ALPHA` | **ひずみ速度依存の «形» だけ** | 公表値の帯（n 4.0〜6.5・α 0.012〜0.050）の中 |
| `LN_A` | **絶対値だけ** | 荷重実績で較正する。**動かしてよいのはここだけ** |

ひずみ速度感受性 `m` は**材料定数ではありません** —— 上の式から解析的に出る導出量です。

```
      m_eff ≡ ∂lnσ/∂lnε̇ = tanh(α·σ) / (n·α·σ)
```

同じ A5052 でも 520 ℃/0.5 s⁻¹ で 0.172、380 ℃/50 s⁻¹ で 0.079（2.18 倍）になります。
`modeltrace` が、式から逆算した Q と n が表の値と一致すること、m_eff が数値微分と一致することを
毎回確かめています。

| 合金 | Q [kJ/mol] | n | α | ln A | kf [MPa] | 熱間域 [℃] | 巻取 [℃] | 端部欠陥係数 | 特徴 |
|---|---|---|---|---|---|---|---|---|---|
| A1050 純アルミ | 156 | 4.5 | 0.040 | 26.202 | 34.3 | 400–550 | 320 | 0.9 | 軟らかく温度依存が小さい |
| A1100 純アルミ（Cu 添加） | 158 | 4.5 | 0.035 | 27.517 | 34.0 | 400–550 | 320 | 0.9 | 実機の較正ロットの材質 |
| A3003 Al-Mn | 180 | 5.0 | 0.025 | 30.106 | 54.6 | 400–540 | 320 | 1.0 | 汎用。8 mm コイルまで余裕をもって成立する |
| A4045 Al-Si | 160 | 5.0 | 0.022 | 27.959 | 51.8 | 380–500 | 300 | 1.5 | ろう材。融点が低く上限が低い |
| A5052 Al-2.5Mg（既定） | 175 | 5.0 | 0.020 | 27.092 | 87.7 | 380–520 | 300 | 1.4 | Mg 固溶で硬い。既定ロットの材質 |
| A5083 Al-4.5Mg | 185 | 5.5 | 0.016 | 26.694 | 127.1 | 380–500 | 300 | 1.8 | 厚板用。8 mm コイルは成立しない（60 mm 板材は成立） |
| A6061 Al-Mg-Si | 145 | 5.0 | 0.022 | 22.588 | 73.8 | 400–540 | 320 | 1.3 | 構造材 |
| A7075 Al-Zn-Mg-Cu | 230 | 5.0 | 0.015 | 34.306 | 146.7 | 360–450 | 300 | 2.6 | 最も硬く熱間域が狭い。8 mm コイルも 30 mm 板材も成立せず、60 mm 板材のみ |

`kf` は比較用の実測値で、**同じパス（26 → 16 mm・420 ℃・80 mpm）を 8 材質で通したときの変形抵抗**です
（`node tools/sensload.mjs`）。**荷重はこの比のまま 583 t 〜 2,582 t ＝ 4.43 倍**変わります ——
パススケジュールを材質ごとに分けるのが «念のため» ではなく必然である理由がこれです。

炉出し温度スライダの範囲は選んだ合金の熱間域に自動で合わせます（範囲外の温度を選ばせない）。

### ラインの向き

`CONFIG.FLIP` が第1パスの進行方向を決めます。寸法はすべて「第1パスが +X」の正準向きで
記述し、`mirrorLayout()` が X 座標とパス方向をまとめて折り返します。個々の箇所で
符号を書き分けないので、入側／出側の左右反転は定数 1 個で切り替わります。

パスラインは**調整装置（ステップウェッジ）付き**の構成としています。圧延中は下ワークロール上面が
パスラインで固定され、圧下はすべて上側（油圧圧下シリンダ）で行います。

---

## 1-2. スラブ装入設備の配置

実機配置に合わせ、転倒機とその周辺（ピット炉・装入クレーン）は **入側テーブル A-8 / A-9 / B-1 の操作側**
（ライン中心から側方 3.8 m、転倒軸はミル中心から 33.0 m）に置いています。スラブはピット炉で立てたまま
加熱され、装入クレーンで吊り上げられて転倒機に立てかけられ、倒されたあと **トランスファークレーン** が
吊り上げてラインへ横移動し、A-9 テーブルに降ろされます。ラインへの移動は **テーブルローラによる送り出しでは
ありません**（縦送り・横送りローラは持ちません）。

工程は `CONFIG.SEQUENCE` の並びがそのまま定義になります（9 工程）。

| # | 工程 | 内容 |
|---|---|---|
| 1 | 蓋開 | ピット炉の蓋をスライドさせて開く |
| 2 | 吊上げ | 加熱済みスラブを立てたまま装入クレーンで炉から引き上げる |
| 3 | 横行 | トロリが転倒機まで横行する |
| 4 | 立てかけ | 転倒機の受け面へ降ろす（この時点でスラブは直立） |
| 5 | 転倒 | 転倒アームが 90° 倒し、ベッド面（＝パスライン高さ）に平置きにする |
| 6 | 送り | 転倒機ベッドのテーブルローラが `FEED_DX = 2,500 mm` だけ出側へ送り、続きの受取テーブル（同じテーブルローラ）へ渡す（下記「2 台のクレーンのフレームを分離した理由」） |
| 7 | 吊上げ | トランスファークレーンが待機位置からスラブの真上へ走行し、トングを降ろして爪で側面を掴み、受け面を越える高さ（パスライン上 1,700 mm）まで吊り上げる |
| 8 | 横移動 | トロリがラインの真上（Z = 0）まで横行する |
| 9 | 降ろし | A-9 テーブルへ降ろし、トングを開いて吊り上げる。ここから第 1 パスが始まる |

| 項目 | 値 |
|---|---|
| 転倒軸位置 | ミル中心から 33.0 m（A-9 / A-8 境界付近）、側方 3,800 mm |
| 転倒軸の高さ | ベッド面（パスライン）より 330 mm 下。軸 Φ560 がベッド面から突き出ない |
| 転倒アーム | 長さ 4,600 mm（最大スラブ長 4,200 mm）、幅 3,100 mm、受け面高さ 1,510 mm。**ベッドはテーブルローラ**（胴長 2,400 mm・ピッチ 640 mm・7 本。形状は本線と同じ `Parts.tableRoll`、軸端だけ 190 mm に詰めてサイドレールに収める） |
| 受取テーブル | 転倒アームの回転半径の外（転倒軸から 4,900〜7,100 mm）に固定で並べた同じテーブルローラ 4 本。上面はパスライン |
| 送り量 | `FEED_DX = 2,500 mm`（転倒直後の近端位置からの追加移動）。上限は «降ろしたスラブが A-9 に載る» ことで決まり ≤ 3,000 mm |
| 降ろし位置 | 倒したスラブの真横（同じ X）。板長 L により `TILTER_X + F·(FEED_DX + L/2)` |
| ピットクレーン（装入クレーン）走行路 | 転倒軸のすぐ脇の門型 1 対（桁は X 方向に動かない固定橋）。桁高さ 12.5 m、Z = −6.5 / +14.0 m |
| トランスファークレーン専用フレーム | 桁高さ 7.5 m・柱断面 450 mm・Z = −2.1 / +6.2 m（転倒アームの占有域にも主テーブル架台にも重ならない位置）。待機位置は転倒軸からミル側 6.5 m（転倒アームが起きる途中に先端が半径 4,600 mm まで張り出すため、トングはその円の外で待つ）。吊りビーム 3,200 mm、トング 2 基（±1,100 mm） |
| トング（板つかみ） | 図面どおり最大巾 2,680 mm・ロッド芯間 1,525 mm・全高 3,355 mm・爪 560 mm。マスト下端の中心ピンに 2 本のアームを吊り、肘ピンをシリンダで押し引きして爪を閉じる |
| ピット炉 | 深さ 5,200 mm（最大スラブ長 4,200 mm を立てて収める） |
| 吊上げクリアランス | 床上 2,600 mm |

**駆動側（−Z）には置けません。** スピンドル、ピニオンスタンド、減速機、主電動機が占有しているためで、
実機でも材料ハンドリングは操作側、駆動は駆動側に分離されます。

### 2 台のクレーンのフレームを分離した理由

装入クレーン（ピットクレーン）は立てたスラブ（最大 4,200 mm）を吊るため桁高さ 12.5 m の
背の高いフレームが要ります。トランスファークレーンは平置きの薄い板を 1.7 m 持ち上げるだけなので、
同じ高さのフレームを共用する理由がありません（共用させていた旧版は、桁からスラブまでのワイヤが
10 m 近く垂れ下がる不自然な絵になっていました）。

フレームを別にすると、今度は**転倒直後のスラブがピットクレーンの脚の内側にあり、低いフレームの
受け持ち域に届かない**という問題が出ます。低いフレームの Z スパンは転倒アームの占有域だけでなく
主テーブル架台（ロール胴端の外側、Z = ±1,545 mm）とも重ならない位置まで押し出す必要があり
（`tools/structure.mjs` のレイキャスト判定で実測）、その結果フレームは転倒軸から離れた位置にしか
置けません。そこで転倒後、**転倒機ベッドのテーブルローラが板を FEED_DX だけ出側へ送り**、その続きの
受取テーブル（同じテーブルローラ）へ渡してからトランスファークレーンが掴む、という 2 段構えにしています。

送り機構をプッシャではなくテーブルローラにしているのは、転倒機ベッドと受取先が **1 本の搬送ライン**
だからです。ローラ仕様は `CONFIG.SUPPLY.ROLLER` の 1 か所に置き、形状は本線と同じ `Parts.tableRoll()`
を胴長と軸端長だけ変えて使います。回転は角度を積算せず、送り距離から転がり条件 θ = −距離 / 半径 で
毎フレーム決めるので、コマ落ちや巻き戻しがあっても板とローラがずれません（`tools/layouttrace.mjs`
がすべり量を実測して 20 mm 未満であることを確認）。

### トングの構造

トングは図面（最大巾 2,680 / ロッド芯間 1,525 / 全高 3,355 / 爪 560）に合わせた挟み込み式です。
マスト下端の**中心ピン**に 2 本のアームを吊り、**肘ピン**をシリンダで押し引きすると両アームが
中心ピンまわりに逆向きに回って爪が閉じます。爪はスラブの**側面（板厚の面）**を掴むだけで下へは
回り込まないので、テーブルローラに載ったままの板をそのまま持ち上げられます。爪先はローラ上面より
`JAW_LIFT = 60 mm` 上で止め、ローラの胴には当てません。

油圧シリンダは**受け梁の両端に鉛直に据え**、胴の中ほどを梁のバンドが抱き、ロッドが下向きに出て
アームの**ロッドピン**へ届きます。ロッドピンはアームと一体なので、アームの回転をそのまま掛ければ
現在位置が決まり、シリンダ本体は梁に固定されたままなので、**ロッドの出代は 2 点間の距離として
自然に決まります**（別に持たせて食い違う、ということが起きません）。

開閉はアームの回転角だけで表しています。爪の内面は中心ピンから半径 R の円周上を動くので、
目標の開き幅に対する角度は `asin` で一意に決まり、開き量・爪先高さ・ロッドピン位置・シリンダの
出代がすべてこの 1 つの角度から出ます。

**簡略化している点:** 図面では «中心ピンから V に開く上リンク» と «そこにピンで繋がる湾曲した
爪アーム» が別部材で、左右をたすき掛けのタイバーが結んでいます。ここでは上リンクと爪アームを
**中心ピンまわりに回る 1 個の剛体**として扱い、たすき掛けのタイバーは省いています（実機の 4 節
リンクを毎フレーム数値解法で解く代わりに、姿勢が一意で食い違わないことを取りました）。
シルエットとシリンダの動きは図面どおりです。

この «全高 3,355 mm のトングを吊る» という条件が、トランスファークレーンの桁高さを決めています。
最も高くなるのは最小幅スラブ（900 mm）を掴んだとき（アームが立つぶん中心ピンが爪先から遠のく）で、
爪先 2,660 → 中心ピン 4,293 → 吊りピン 6,398 → 吊りビーム上面 +420 → ロープ最小 300 と積み上がり、
桁高さは 7,500 mm 必要でした（ピットクレーンの 12,500 mm よりは十分低い）。

### 成立条件（自動検査している項目）

- 降ろしたスラブ（最大 4,200 mm）が A-9 テーブルの範囲に収まり、3 本以上のローラに載る
- 転倒機ベッド・受取テーブルのローラ上面がパスラインに一致し、受取テーブルは転倒アームの回転半径の外にある
- ローラの回転が送り量と一致する（転がり条件・すべり < 20 mm）、送り量が `FEED_DX` に一致する
- 横移動時のスラブ底面（パスライン上 1,700 mm）が転倒機の受け面（1,510 mm）より上
- 全開のトング（外面 1,340 mm）が転倒アームのサイドレール内面（1,390 mm）の内側に収まり、
  爪内面（1,140 mm）は最大幅スラブの半幅（1,100 mm）より外にある（＝板を跨げる）
- 2 台のクレーンの桁が待機時に重ならない（桁間 ≥ 760 + 最大スラブ長/2）
- 装入の全工程でスラブが装入設備（受取テーブル・両クレーンを含む）に食い込まない（`tools/supplytrace.mjs`、分離軸判定）
- 静的構造物どうしが食い込まない・浮いていない（`tools/structure.mjs`）。トランスファークレーンの
  低いフレームを主テーブル架台・転倒装置とぶつからない位置まで押し出したのはこの検査で確定させた
- **トング（可動部）が転倒アーム・受取テーブルと食い込まない**（`tools/tongtrace.mjs`）。
  既存の検査は «静的部材どうし» と «スラブと設備» しか見ておらず、«動く設備と他の設備» が
  抜けていた。爪を下ろす先がローラと側枠の間なので、この穴は塞いでおく必要がある
- **クロップ屑の払い出しが機械として成立する**（`tools/scraptrace.mjs`）。下台の全ストロークで
  固定ストリッパシュートが下台を貫かないこと、受け開口がテーブル架台の足元を掘っていないこと、
  先端・後端の屑が段階どおりに進んで同じ屑箱に収まることを機械判定する

### 転倒装置とスラブの干渉を無くした構造

以前は転倒軸（Φ560）の軸心がベッド面（パスライン）にあり、寝かせた板にも立てた板にも
軸が 280 mm 食い込み、アームには床下 1.5 m まで潜る «支持脚» が付いていました。現在は

- 転倒軸をベッド面より **330 mm 下**（軸半径 280 + 余裕 50）に置く。寝かせたときベッド面は
  パスライン、立てたときの受け面は軸から 330 mm 離れた平面になる
- 立てた板は受け面に寄りかかり、底面は **リップ**（寝かせたときの立ち上がり板）に載る。
  リップは軸の真上（y ≥ 320）から立つので回転しても軸に当たらない
- サイドレールは軸受（Φ940）の外側から始め、軸ハブ（Φ640）で軸を抱く
- 寝かせたアームは床上の受け台に載る。基部・軸受台・転倒シリンダの受けも床上

転倒中の板の位置は、アームのローカル系で板中心 (L/2, 330 + t/2) を軸まわりに回すだけで
決まり、立位・倒位の両端で立てかけ位置・搬送位置に一致します。
装入の全工程でスラブが装入設備に食い込まないことは `tools/supplytrace.mjs`
（回転する板と設備の三角形の分離軸判定）で確認しています。

**ラインへの移載はローラ送りではなくトランスファークレーンです。** 以前は昇降式の横送りローラで
ラインへ渡していましたが（ジオメトリに昇降ステムが含まれて回転で振れる、回転向きがラインの向きの
反転を考慮していない、という不整合があった）、この機構は撤去しました。現在テーブルローラが担うのは
**転倒機ベッド上と受取テーブルまでのライン方向（X）の送りだけ**で、ライン中心（Z = 0）へ渡すのは
トングを持つトランスファークレーンです。

---

## 1-3. 出側仕上げ設備の動き

出側には、ミル中心から順に **サイドトリマー（5,875 mm）→ スリーロール（6,900 mm）→
巻取リール（8,350 mm）** が並び、さらに遠方（ワークロール出側から約 30.6 m）に
**75 mm クロップシャー**があります。距離が大きく違うのは役割が違うためで、
一体の設備ではありません。

| 設備 | いつ働くか | 何をするか |
|---|---|---|
| 75 mm クロップシャー | 板厚が 75 mm 以下になった最初のパス | 先端のワニ口・ラウンドを切り落とす。**上刃固定・下台上昇式**（板押え無し）で、1 カット 300〜500 mm を欠陥が消えるまで繰り返す。**出側のみ**に存在する |
| サイドトリマー | 最終パス（コイル仕上げ時） | 両耳を切り落として仕上げ幅にする。切った耳材はスクラップチョッパで細断される |
| スリーロール | 通板時 | 下2本・上1本で板を挟み、リールへの通板経路をつくる |
| 巻取リール | 最終パス（板厚 10 mm 以下） | マンドレルを拡張して先端を把持し、張力を掛けながら **外周に 1 枚ずつ** 巻き取る |
| 30 mm シャー | 板材の仕上げ（板厚 30 mm 以下） | J-3 テーブルの先（80.9 m）。下刃固定・上刃下降式（板押え付き）で、板を 9 m 定尺に切り分ける |
| パイラー | 板材の仕上げ（板厚 75 mm 以下） | 30 mm シャーの先（82.3〜92.3 m）。切り分けたシートを片持ちローラアームで受け、アームが左右へ開いてリフトテーブル上の山へ落とす。山が高くなるぶんテーブルが下がる |
| 板置き場（クレーン） | 板材の仕上げ（板厚 75 mm 超） | どちらのシャーでも切れないので J-3 末端で止め、クレーンで駆動側の板置き場へ降ろす |

### 仕上げ形態は板厚で決まる

目標板厚は 4〜150 mm で自由に設定でき、**10 mm 以下ならコイル、10 mm 超なら板材** と
自動で振り分けます（`Rolling.finishMode`）。板材はさらに板厚で行き先が決まります
（`FinishState.divider`）: **30 mm 以下は 30 mm シャー、75 mm 以下は 75 mm シャー**で 9 m 定尺に
切り分けてパイラーへ、**75 mm 超**は切れないので J-3 末端からクレーンで板置き場へ。切り分けは
«シャーの先（定尺 + 隙間）にシートが残っていない» ときだけ板を送り、切り離したシートはパイラーの
停止位置（または前のシートの手前）で止まって順に積まれます。板の尾端を含む最後のシートは切らずに
板そのものを送り、尾端の張り出し（舌）を保ったまま山に載ります。パススケジュールは板厚・板幅・材質から
その場で生成され、待機中の変更は «適用» を押さなくても即座に反映されます。
巻取中は圧延後の板がすべてコイル側へ行き、出側テーブルへは流れません
（以前は板メッシュが 3ロールを越えて出側テーブルにも描かれ、板がコイルと出側に二分して
見えていました。現在は板の入口 A（3ロール出側ローラ上）で打ち切り、そこから先は渡り板と
コイルだけが描かれます）。

**サイドトリマーはシャーではありません。** スタータイプのスクラップチョッパ付き
クイックオープン型で、円形刃（Φ510〜498、厚 40〜30 mm）が板を連続的に走間切断します。
このため刃は板の通過速度に同期して回転させ、断続的に落ちる動作にはしていません。
「クイックオープン」は非通板時に刃を素早く開いて板幅方向へ退避させる機構で、
モデルでも通板前に刃を開き、切断時のみ閉じる動きにしています。

**75 mm シャーは上刃固定・下台上昇式**です。板を載せた下台（下刃付きの昇降テーブル、
長さ 1,800 mm）ごと油圧で持ち上げ、固定上刃（刃先はパスライン上 90 mm）に押し当てて
剪断します。板押えはありません。上刃が固定なので架構は上刃ホルダを受ける高さだけで済み、
クロスヘッド上面はパスライン上 1.1 m に収まります（以前のアップカット構造は 5.6 m ありました）。
刃は実機仕様の **75 × 180 × 2500 mm（NS3C, Hs 65±3°）**、最大剪断力は **368 ton** です。

### 端部の欠陥長と複数カット

切り落とす量は先端の欠陥（ワニ口・舌・ラウンド）の長さから計画します。

```
欠陥長 D = 圧下率² × 材質係数 × (1 + 0.8·低温度合) × (3.0·板厚 + 0.30·板幅)
必要量  = D + 余裕 120 mm   →  1 カット 300〜500 mm に等分（端数切り上げ）
```

板厚 60 mm 換算で、全材質・全幅・熱間域の上限〜下限にわたり総切断長は **410〜2,400 mm**
（1 カット 300〜500 mm）になります。切り上げのぶん必要量より少し余分に切るので、
ワニ口の根元が残って 2 枚板になることがありません。切断は
STOP → ALIGN（端を刃からカット長だけ先に置く）→ CUT（下台上昇）→ RETURN → 払い出し
を、計画したカット数だけ繰り返します。払い出しは端で分かれます。

- **先端** … EJECT → RETRACT。端材は下台と一緒に持ち上がった高さから固定エプロンへ落ち着き、
  駆動側のプッシャで操作側のコンベアへ払い出され、パレットに積まれます
- **後端** … BEDDOWN → STRIP → BEDUP。端材は下台に載ったまま降り、下台のテーブルの通し溝を
  貫いて固定されたストリッパシュートに置き去りにされて上流側へ滑り落ち、ライン下の端材ピットへ
  入ります。傾斜コンベアがテーブル架台の下をくぐって駆動側へ運び、末端の端材箱へ落とします

### 定格 368 t が決める剪断応力

368 t は最大断面（板厚 75 mm × 刃長 2500 mm）を剪断する力なので、その裏にある剪断応力は

```
τ = 368 tf ÷ (75 × 2500 mm²) ≒ 19.2 MPa
```

熱間アルミの剪断強さとして妥当な値です。モデルはこの τ から実際の断面での必要剪断力を
逆算し（`Rolling.cropForce`）、切断のたびに表示します。板厚 75 mm × 巾 2350 mm（トリム前最大巾）
でも 346 t で定格内に収まることを自動検査しています。

### 構造の成立条件（自動検査している項目）

- 下台の行程（90 + 60 mm）で下刃先が固定上刃の刃先を越える（切り離せる）
- 固定上刃の刃先はパスライン上 90 mm で、最大板厚 75 mm を通す
- 剪断面を挟んで、下台（下刃）が上流側・固定上刃が下流側（＝端材が残る側）
- 上下刃のサイドクリアランスは 4 mm（板厚 75 mm の 5.3 %）
- 架構の最上部がパスライン上 1.5 m 以内（コンパクト）、板押えを持たない
- 下台は柱の内側の昇降ガイド柱に案内され、床上のシリンダ本体の中をロッドが伸縮する
- 1 カットは 300〜500 mm、総切断長は 300〜2,400 mm（板厚 60 mm・全材質・全幅・温度域）

### 屑の落ち方（エプロンを持たない理由）

刃の出側にはエプロンを置いていません。切った屑をその場で**テーブルロールの下へ落とす**ためです。
先端カットでは屑は支えを失って開口へ落ち、後端カットでは下台の上に残るのでシュートで移します。
どちらも受け面（傾斜コンベアのデッキ）に着床してから操作側へ運ばれ、末端で屑箱へ落ちます。
コンベアの桟は屑が載っている間だけ送られます。複数の屑が同時に搬送中でもかまいません。

出側へ «滑らせて» 出す案は採れません。下台の刃物台が正準 X −197〜+118 を上下に掃くので、
その回廊に固定のシュートを置けないからです（実測）。後端の屑を上流側へ落とすのはこのためです。

### 屑は切断時の形を保つ

端部の張り出し（舌＝幅中央が伸びる／ワニ口＝表裏が開く）は `Rolling.endOffset` の 1 式で、
板（`SlabView`）・端材・シートのすべてがこれを参照します。切り離した端材は
`Parts.plateSegment` が **切断時の形を頂点に焼き込んだ個別メッシュ**なので、板端の変形が
そのまま端材に残ります。刃が張り出しの途中にあるときは、板端はその位置で平らに打ち切られ
（`SlabState.cut`）、残りの張り出しは次のカットへ持ち越されます。したがって 1 カット目の端材は
舌の先端、2 カット目は舌の根元を切った形（切断面と切断面に挟まれた片）、最後は健全部まで
切り込んだ平らな片になります。切断面の位置は «健全端からの残り長さ» で持つので、板が動いても
一緒に動きます。

### 端材と体積収支

切り落とした端材のうち、**張り出し（舌・ワニ口）は形状のひずみなので板長としては減りません**。
板長として減るのは健全部だけです。

**体積保存の «法» は «板 ＋ 切り落とした材料 ＝ 素材»** であって「板だけ」ではありません ——
クロップで落とした材料は板から消えるのが正しいので、**板だけの増減は «どれだけクロップしたか»**
を測っているにすぎません。既定ロット（530 × 1,330 × 3,450）の実測では

- **板 ＋ 切り落とし ＝ +0.8 %**（保存されている。切り落とし 4 回・板長にして計 1,484 mm）
- 板だけ ＝ −4.5 %（＝ クロップで落ちた割合）

`tools/voltrace.mjs` が毎回この 2 つを出します。素材長を変えると**一部の条件で体積が «作られる»**
という未解決の不具合が残っています（0-4 の 21）。

**巻取リールは 4セグメント・コラプシブルウェッジ式**です。マンドレルはウェッジ芯の上に
4 枚の円環扇形セグメント（外径 Φ610、肉厚 95、隙間 26 mm）が乗り、先端が 3ロールの手前
3 m に来たら **半径方向に 30 mm 拡張**（0.8 s）します。マンドレルは拡張・縮小するだけで
**板を掴む機構を持ちません**。巻き終えて（コイルを抜くときに）縮小して解放します。
セグメントは各自の二等分線方向に動くので、外接径の減少は 2 × 30 × cos45° ≒ 42 mm です。

### ベルトラッパー（巻き始め）

マンドレルが板を掴まない以上、**巻き始めを作るのはベルトラッパー**です。3 本のラッパーロール
が揺動アームに載り、その外側にベルトが掛かります。ロールが内側へ閉じるとベルトが
マンドレル外周に沿い、先端はベルトとマンドレルの間に入って摩擦で連れ回されます。
数巻きしてコイルが自分で形を保てるようになったらアームが開き、ベルトは離れます。

**常設の設備**です。使わないときは退避位置へ振り上げるだけで、消えることはありません
（初期の実装は «巻き付けている間だけ現れて終わると消える» もので、設備としては存在して
いませんでした）。

機構は 2 段になっています。

1. **各ロールはキャリッジ上の «接線方向の支点» に付いた短い剛体アーム**に載り、
   コイルが太るぶんだけ外へ逃げる（1 周ごとの段にも追従する）
2. **キャリッジそのものが 1 本の振り軸まわりに 55° 振れて**、ロール・アーム・ベルトを
   まとめて上へ退避する

②が要る理由は測って分かりました。可逆圧延機なので**板はどのパスでもリールの下（パスライン）を
通ります**。巻き付けの位置は «板の入口の直後» ＝ マンドレルの下側なので、そこから半径方向へ
開くだけではロールがパスラインまで下がり、`interfere.mjs` が **最大 443.7 mm の食い込み**を
検出しました。板の通り道（パスライン 900 + 最大板厚）を空けるには、まとめて振り上げるしか
ありません。

| 諸元 | 値 | 備考 |
|---|---|---|
| ラッパーロール | Φ240 × 面長 1,800、3 本 | 極角 212 / 300 / 25°（閉じたとき。真上が 90°） |
| 揺動アーム | 接線方向へ 700 mm の支点、アーム長 700 | **長さの変わらない剛体**。閉じたときアームはマンドレルに接し、開くほど遠ざかる |
| キャリッジ | 振り軸はリール中心から (−1,900, +200)、退避角 55° | ミルと反対側。そこだけ床も上空も空いている |
| ロール中心半径 | 閉 425（＝マンドレル半径 305 + ロール半径 120）／ 退避 1,389〜2,138 | 退避はロール面が最大コイル Φ1,900 の外 |
| ベルト | 厚 18、面長 1,800 | 3 本のロールに掛かる帯（円弧 + 共通外接線）を毎フレーム作る |
| 架台 | 振り軸 + 駆動側の柱 1 本 + 振れ止め | 床へ降ろすのは**駆動側だけ**（操作側の床はコイルカーの通り道） |

- アームは剛体なので、置きたい半径 r を決めれば揺動角が決まります（`|C|² = |P|² + L² + 2L(P·u)`
  を u について解く。`Wrapper.phiFor`）。支点を**接線上**に置くのが要点で、こうすると
  «閉じたときアームがマンドレルに接し、開くほど遠ざかる» 形になり、腕が芯を横切りません。
  `wraptrace.mjs` が全工程でアーム長が 700 mm から 1 mm も動かないことを見ています
- 退避は**揺動角で補間**します。半径で補間すると、円の上では同じ半径が 2 回現れるため
  «別の枝» に乗って腕が芯へ回り込みます
- 架台の Z は «コイルカーの走行域（±1,075）より外、テーブル架台（±1,325）より内» の
  200 mm の帯に収めています。操作側の床にはコイルカーが走るので、柱は駆動側だけです
- 順序は «拡張 → ベルトを閉じる → 先端が入る → 巻き付く → 3 巻き後に開く»。
  «閉じ切ってから通板» でなければ巻き始められないので、`coiltrace.mjs` が
  巻き付き開始時の閉じ度が 1.0 であることを検査している
- ロールは押えコロと同じく、**その極角でのコイルの実半径**（1 周ごとの段を含む）に当てる。
  平均半径に当てると 1 板厚ぶん浮くので、検査は隙間 3 mm 未満を要求する
- 旧版はマンドレルが先端を «把持» する実装で、実機に無い機構だった（板の挙動が不自然だった）
**駆動側に主軸受、その背後に減速機と電動機を同軸で並べ、操作側はアウトボードベアリング**という
片持ち構成で、操作側はコイルを抜き取れるよう開いた柱だけの構造です。
実機は鉄板で囲われた箱ではないので、モデルでも囲いは設けていません。

### コイルカー（巻き終えたコイルの払い出し）

リールは片持ち構成で、**操作側はアウトボードベアリングの柱 2 本だけの開放構造**（コイル抜き取り側）
です。巻取が終わると、リール真下のコイルカーが受け台を上げてコイル外周に当て、マンドレルを
縮小してから、その柱間（内法 760 mm）を通して**軸方向（操作側）へ 6 m 抜き取り**、降ろします。

- 受け台の上昇端は固定ストロークではなく、**コイル外周がデッキに載る高さ**
  （マンドレル芯 − コイル半径）から逆算する。コイル径は巻取結果で変わるので、
  固定ストロークにすると浮くかめり込む
- マンドレルは **カーが受けてから** 縮小する（先に縮めるとコイルが落ちる）。
  `coiltrace.mjs` が工程の順序（APPROACH → LIFT → STRIP → CARRY → SET → REST）を検査する
- **待機はリール下ではなく搬出位置**。出側テーブルはリールの «先» まで続くので、
  圧延中はリール下を板が通る。そこで待つとカーが板と当たる（`tools/interfere.mjs` が
  176 mm の食い込みとして検出した）。巻き終えてから APPROACH で入ってくる
- **出側テーブルはリールの手前で終わる**。以前は «ﾃﾞﾌﾟﾚｯｼﾝｸﾞ» 区分としてリール下に
  ローラ 2 本があり、コイルカーが入る余地が無かった（ローラ間の隙間は 368 mm しかない）

### クロップ屑の払い出し（入側・出側 → ラインの下の受け → 傾斜コンベア → OS の屑箱）

屑は必ず «刃を挟んで切る端の側» に落ちます。先端カットの屑は刃の**出側**に、後端カットの屑は
**ミル側 —— 下刃を載せた昇降下台の上**に残ります。以前はこれを 2 系統（先端＝エプロン＋
プッシャで OS へ、後端＝シュートで DS へ）に分けていましたが、**1 系統に統一**しました。
エプロン・プッシャ・ローラコンベア・パレットは撤去しています。

受けの開口は、シャーまわりで «床に着いているもの» を実測して決めました
（正準 X ＝ シャー中心から下流が +）。

| 部材 | 正準 X | Z |
|---|---|---|
| 昇降下台（掃く範囲） | −717 〜 +118 | ±1,570 |
| 下台の昇降シリンダ | −397 ±180 | ±1,500 ±180 |
| 下台の昇降ガイド柱 | −672〜−522 / −272〜−122 | ±1,750 ±75 |
| 75 mm シャーの柱 | −550 〜 +50 | ±2,400 ±380 |
| テーブル架台（床に接する足） | ローラごと | ±1,325 〜 ±1,765 |
| テーブルローラ（胴端 Φ281.6） | −1,175 と +800 | — |

⇒ **開口は 正準 X −1,030〜+650、Z −1,150〜+1,150**。刃の入側・出側の両方の真下に跨がり、
最大板巾 2,200 の屑が落ちる 2,300 mm を確保しつつ、床に着いているものを 1 つも掘りません。

- **広い傾斜スクラップコンベア（桟付き）**: 受け面そのものがデッキ（巾 1,600 mm）。
  ピット底（Z −1,050 でパスライン下 2,400 mm）から Z +9,000・パスライン上 72 mm まで、
  勾配 ≒14°。勾配は «床でふさいだ区間（Z 1,150〜2,900）を屑と桟が頭を打たずに通れる»
  ことで決まります。ふさいだ区間の下でテーブル架台とシャーの柱の足元をくぐります
- **屑箱**: 操作側の床上（Z 10,200、3,600 × 1,900 × 250 mm）

後端の屑だけは下台の上に載るので、**固定ストリッパシュート**で開口へ移します。フィンガは
下台のテーブルに空けた通し溝（Z = ±300 / ±700、溝幅 240 mm）を貫いて架構に固定されており、
下台が DROP（240 mm）だけ降りると屑だけがシュートに残ります。傾斜は
tan⁻¹(220 / 530) ≒ 22.5° で、鋼どうしの摩擦角（μ ≒ 0.25 → 14°）より大きいので自重で滑ります
（滑り出しの加速度 a = g(sin θ − μ cos θ)）。

シュートの下端とコンベアの頭部では、屑は **«支点を中心にこけてから落ちる»** 動きにしています。
重心が支点を過ぎた時点から回り始め、支点に当たる点が重心から後端へ移っていくので、板の後半は
最後まで支持面より上に居ます。平行に落とすと後半が支持面を突き抜けます（実測 517 mm）。

この構成は «固定部材（シュート）が可動部材（下台）を貫く» という、既存のどの検査も見て
いない形をしています。そこで `tools/scraptrace.mjs` で、**下台の全ストローク（上昇 +150 〜
下降 −240）でシュートが下台を貫かないこと**、開口が架台の足元を掘っていないこと、先端・後端の
屑がそれぞれ ONBED→CHUTE→CHUTE_TIP→PITFALL→CONVEY→DROP→REST ／ PITFALL→CONVEY→DROP→REST と
進んで同じ屑箱に収まること、搬送中に固定設備へ食い込まないことを機械判定しています。

### テーブル区分と E-1 / E-2（ミル直近の短胴ローラ）

ローラの配置は図面の「各テーブルローラ本数」（区分ごとの本数とピッチ表記）と区間長から
`Layout.section()` が導きます。区間長がピッチの合計とほぼ同じ区分（D で余り 5 mm、F で 15 mm、
H-1 で 0 mm）が多いので、収まるかどうかは **«ピッチの合計が区間長以下か» だけ**で判定します
（以前は «余りが胴端半径 + 20 mm 以上» を求めていたため、D・F・G-1・H-1 が軒並み等配へ落ちて
図面のピッチが消えていました）。余りが半ピッチを超える A-1 だけは、ミル側へ半ピッチで寄せて
隣の区分との継ぎ目が空きすぎないようにします。

**E-1 / E-2**（図面の 0〜2.5 m、ミルの直近）は他と造りが違います。

| 項目 | 値 |
|---|---|
| ローラ | Φ380 × 857L（SCM440）。中央は 1/100 の勾配で Φ377.1 |
| 本数 | 各 8 本 ＝ **4 段 × OS/DS 2 本** |
| 段の位置 | 正準 X 751 / 1,217 / 1,683 / 2,149（ピッチ 466） |
| 胴の外端 | Z = ±1,100（最大板巾 2,200 の半分）。内側の首は中央の 1 個の軸受で受ける |

段の位置は **ワークロール（Φ1,350）との軸間** と **D / F の 1 本目（正準 2,505、胴端 Φ281.6）**
との逃げで決まります。床はミルピットの開口なので脚は立てられず、ハウジングの柱間へ渡した
リブでローラの下をくぐらせて支持しています。周速を主テーブルと合わせるため、角速度は径の比
（281.6 / 380）で回します。これに伴い旧「ミル直下の敷板（エプロン）」は撤去し、下側の
ロールクーラントヘッダは E ローラの下をくぐる高さへ下げました。

配置が図面どおりかは画面上でも確かめられます。ツールバーの **「テーブル記号」**（<kbd>Z</kbd>）で
区分ごとに **記号バッジ**（区分名・本数・ピッチ表記）が出ます。バッジは «実際に並んだローラの
範囲の中央» に置くので、配置がずれていればバッジもずれて見えます。

### 1 枚ずつ巻き太るコイル

コイルは «外径が連続に太る円筒» ではなく、**外周に 1 枚ずつ板が巻かれて 1 周ごとに
板厚ぶん段になって太る**形で描きます（`CoilGeometry`）。

- 巻き数 = 板の周速 ÷ 巻き付け半径（今巻いている層の板厚中心）の積分。マンドレル 1 回転 = 1 巻
- 完成した層は内径 Φ610 〜 `r0 + 層数·h` の環（全周）
- 巻きかけの層は `r1 〜 r1 + h` の扇形で、板の入口から回転方向へ 2π × 端数だけ広がる。
  扇形の先端が **段** で、マンドレルが 1 回転するごとに入口へ戻ってきて次の層が始まる
- 板の入口 A（3ロール出側ローラ上の板厚中心）からコイルへは、巻き付け円への **接線** で入る。
  接点は円の下半分（**下巻き**: 板は下から入って奥→上→手前と回る）。コイルが太るほど
  渡り板の立ち上がり角は寝る（Φ610 で約 30°、Φ1,510 で約 16°）
- 尾端が入口 A を過ぎたら渡り板は A 側から縮み、接点に達したところで巻取完了

巻き数の積分と面積式の外径は 3 % 以内で一致し（8 mm × 56 巻 → Φ1,506 対 図面 Φ1,510）、
既定条件では 58 巻・Φ1,548 になります。以前の «渡り板がコイルの肉厚中心に刺さる» 見え方は、
渡り板の終点を外径の 35 % という比率で置いていたためで、現在は接線の幾何で決めています。

### コイル押え

押えアームは **サイドトリマー架構の上部梁** から下流へ出したブラケットを支点（トリマー中心から
700 mm・パスライン上 1,700 mm）に揺動し、先端の **金属コロ（Φ200 × 面長 600）** がコイル外周に
接します。下巻きなので狙いは巻き終わり側の **上寄り**（コイル中心から見て真上を 90° として
105〜125°）で、コイルが太るほどアームが起き上がります。コロが当たる極角に巻きかけの層が
あれば 1 枚ぶん外側に乗るので、1 周ごとの段をコロが拾います。駆動は梁上の受けから吊る
シリンダで、巻取をしないパスでは最大コイル径に相当する位置まで退避します。

### 図面「3ロール・板接触角」から取った寸法

| 項目 | 値 |
|---|---|
| リール中心の高さ | パスライン上 **1,200 mm** |
| リール中心 ← 3ロール | **1,450 mm** |
| 3ロール ← トリマー | **1,000 mm**（ミル中心からの 6,900 − 5,875 = 1,025 と整合） |
| 3ロール | 下 2 本 Φ230（軸間 260 mm）＋ 上 1 本 Φ320 |
| トリマー刃 開位置 | 軸心がパスライン ±305 mm |
| 最大コイル径 | Φ1,510（8 mm × 56 巻 / 10 mm × 45 巻） |

この図面は「板接触角」、つまりコイルが Φ610（巻き始め）から Φ1,510（巻き終わり）まで
太る間に、3ロールからリールへ向かう板の角度がどう変わるかを示しています。
3ロールからリール中心へは水平 1,450 / 垂直 1,200 mm なので、板の立ち上がり角は
コイル接線の位置によって **約 30〜40°** の範囲で変わります。モデルの渡り板も
3ロール上端とコイル外周を結んで描いているため、コイルが太るにつれて角度が寝ていきます。

**図面の注記がモデルの外径式を裏づけました。** 「8 mm × 56 巻」を長さに直すと 186.1 m で、
モデルの `OD = √(ID² + 4·L·h/π)` に入れると Φ1,506（図面 Φ1,510）。自動検査に入れてあります。
検証運転で得られるコイルは 8.1 mm × 196.2 m → Φ1,553 で、図面の巻数条件のすぐ外側に
乗っています。

なお **Φ1,900 は巻取機そのものの能力上限**、**Φ1,510 はこのパスラインでの最大径**で、別の値です。
リール高さ 1,200 mm は、Φ1,510 ならコイル下端がパスラインの 445 mm 上、
能力上限の Φ1,900 でも 250 mm 上に残る高さで、**巻取中でないパスの板がリールの下を
通過できます**。可逆圧延機で巻取機を持つ場合に必要な離隔で、自動検査でも両方を判定しています。

---

## 2. 圧延モデル

`Rolling` オブジェクト（純関数の集合）に集約しています。状態を持たないため単体で検証できます。

| 量 | 式 |
|---|---|
| 接触弧長 | `Ld = √(R·Δh − Δh²/4)` |
| 変形抵抗 | `σ = (1/α)·asinh[(Z/A)^(1/n)]`（Sellars–Tegart の双曲線正弦則）。`Z = ε̇·exp(Q/RT)` は Zener–Hollomon パラメータ。材料定数は Q（温度依存だけ）・n と α（ひずみ速度依存の形だけ）・A（絶対値だけ）で、役割が分かれている |
| 変形抵抗の室温上限 | `kf / (1 + (kf/KF_MAX)^p)^(1/p)`,  p = 20。単一の (Q, α) で室温まで外挿できる保証は無い（動的回復が止まって変形機構が変わる。実測で室温の 1.7〜6.1 倍）ので、室温の変形抵抗 KF_MAX を上限として押さえる。p は «角の丸さ» で、p = 4 では上限のはるか手前から効いて熱間域を最大 9.4 % 削っていた。p = 20 なら熱間域の削りは 0.38 %、室温はちょうど KF_MAX に載る |
| 摩擦係数 | `μ = MU·exp(−k_T(T−450))·(V_ref/v)^k_v`（0.14〜0.42 で頭打ち）。温度が上がるほど・速度が上がるほど下がる |
| 中立点・前進率 | `φn = (φ/2)(1 − φ/(2μ))`、`f = R'·φn²/hOut`。出側の材料はロール周速より f だけ速く出る（質量流に反映）|
| ロール表面温度 | 板から受け取った熱でロール胴が温まり、クーラントで待機温度へ戻る。予測と運転が同じ収支を見る |
| 平均ひずみ速度 | `ε̇ = (v/Ld)·ln(hIn/hOut)` |
| 接触弧内のひずみ速度 | `ε̇(φ) = 2·Q_flow·tanφ/h(φ)²`（出口 0・入口最大）。変形抵抗は弧を 8 分割した中点法で平均する —— 平均 1 点で評価すると asinh の凸性のぶん **全パスで 3.6〜3.9 % 高く**出る |
| 圧力分布 | Bland–Ford のスラブ法。`H(φ) = 2√(R/h_out)·atan(√(R/h_out)·φ)` として、入側 `p = k'·(h/h_in)·exp(μ(H_α−H))`、出側 `p = k'·(h/h_out)·exp(μH)`、中立点 `H_N = H_α/2 + ln(h_out/h_in)/(2μ)` |
| 実接触弧長（ロール扁平） | `Ld = √(R'·Δh − Δh²/4)`、`R' = R(1 + C·P'/Δh)`、`C = 16(1−ν²)/(πE)`（Hitchcock）。荷重 → 扁平 → 接触弧 → 荷重 の陰的関係を緩和つき不動点反復で解く |
| 圧延荷重・トルク | `F = w·R·∫p dφ`、`M = w·R²·[∫入側 τ dφ − ∫出側 τ dφ]`。**同じ分布から出す**ので、トルクは荷重の定数倍ではなく別の情報を持つ（m と μ を分けて同定できる）。荷重には接触弧の «埋まっている割合» φ を掛け、噛み込みと尻抜けの Ld/v の間だけ 0→1 に立ち上げる |
| 界面のせん断応力 | `τ = min(μ·p, k)`、`k = k'/2`（凝着）。μ·p が材料のせん断降伏を超えると界面はすべらず凝着する。**荷重には効かず、トルクにだけ効く** |
| 実ロールギャップ | `h = S + δ(F)`（S: 圧下位置）。ミル定数は**一定ではない** —— 実機の測定値（伸び 0〜1 mm 226／1〜2 244／2〜3 267／3〜4 281／4〜5 296 t/mm）を増分ばね定数の表として持ち、`Rolling.millStretch` が表から伸びを引く |
| 塑性係数 | `Q = −dF/dh_out` [t/mm]。«板を 1 mm 薄くすると荷重が何 t 増えるか»。解析式が無い（変形抵抗・摩擦丘・ロールの扁平がすべて h_out に依る）ので荷重モデルそのものを数値微分して取る。AGC のフィードフォワードと幅方向のたわみ（クラウン）が同じ 1 か所を見る |
| 板厚制御（AGC） | ゲージメータ（荷重 FB）: `S = h_ref − δ(F)/(1+誤差)`（制御が思っているミル定数には数 % の同定誤差がある）／モニタ（X 線 FB、輸送遅れつきの積分）／フィードフォワード（入側の板厚偏差を先読み。受け持ちは `δ·Q²/(M(M+Q))`）の 3 段構え |
| 長手方向の板厚 | 材料座標 240 分割。各パスは «出たときの実ギャップ» を書き戻し、次のパスがそれを入側厚として読む |
| 端部欠陥（舌） | `D_t = r²·(材質係数)·(1 + 0.8·冷え)·(3·h + 0.3·w)/2`。r は **その端を最後に平らにしてからの圧下**。幅中央がいちばん伸びる（`1 − nz²`）。どのパスでも付き直す |
| 端部欠陥（ワニ口） | 同じ式だが、**Δ = hMean/Ld > 1 のパスでしか育たない**（表層集中変形）。表裏がいちばん開く（`ny²`）。薄板では出ない |
| 幅方向の板厚（クラウン） | 影響係数法。`(I + Q_w·G)·x = G·p̄ + H_b·F_b + crown`。G は «WR 自由端梁 ＋ BUR 単純支持梁 ＋ 線接触» の連立から作る |
| 平坦度（残留応力） | `λ = ln(h_in/h_out)` の幅方向のばらつき。`σ = −E(λ−λ̄)`、`I = (λ−λ̄)×10⁵`。座屈は `σ_cr = kπ²E/(12(1−ν²))(h/b)²` |
| 形状制御 | 目標クラウン `C* = (C_in/h_in)·h_out`（比例クラウン）。`F_b ← F_b + (C* − C)/g`、g はベンディングのクラウン利得 |
| 熱クラウン／摩耗クラウン | ロール半径の 中央 − 端。中凸はギャップを閉じるので板厚への寄与は `−2(研削 + 熱 − 摩耗)` |
| 幅方向の温度 | `ΔT_edge = h_e(T−T∞)t/(ρc_p√(a·t))`、端からの距離に `exp(−d/√(a·t))` で減衰 |
| 幅広がり | `ln(w₁/w₀) = C·exp(−k·w₀/L_d)·ln(h₀/h₁)`（Helmi–Alexander 型） |
| パスライン調整 | `κ_pl = −3δ/L²`（δ: ロール組の上げ量、L: バイト出口から最初のテーブルローラまで）。バイト内の曲率に足してからスプリングバックへ渡す |
| スプリングバック | `κ_e = 2σ_y/(E·h)`、`M = M_p(1 − (1/3)(κ_e/κ)²)`、`κ_perm = κ − M/(EI)`。`κ ≤ κ_e` なら全部戻る |
| 蛇行（くさび） | `Δh = 2·[δ(S/2+w/2) − δ(S/2−w/2)]·P + ゼロ点誤差 − 圧下の傾き`（合力が中心から e ずれた単純支持梁のたわみ差）|
| 蛇行（キャンバー） | `κ_c = (ln(h_in/h_OS) − ln(h_in/h_DS))/w`。横行は `dψ = κ_c·dL`、`dz = ψ·dL` |
| 差荷重制御 | `tilt ← tilt + KP·Δh`（レート・可動範囲つき）。くさびを打ち消す向きに圧下を傾ける |
| 据わり（片側接触） | 「接触は押すだけ」の下で位置エネルギー最小 ＝ 点 (u, −b) の上側凸包を重心で評価。傾きは包絡線の勾配 |
| 端部の冷え | 端面からの放熱（1/Δx）＋ 端が薄いぶんの割増（1/(1−taper)）＋ 炉出し時点の落ち込み。長手の熱伝導で内側へ広がる（平均 0 に正規化して «分布» だけを持つ）|
| 主機の負担 | トルクは上の圧力分布から出す（`M = 2 × Bland–Ford のモーメント`）。主機のトルクカーブは実測値（最大 1,520 kN·m・ベース速度 75 mpm まで一定、そこから定出力・最高 160 mpm・過負荷耐量 225 %/60 s） |
| トルク収支 | «圧延 ＋ 軸受 ＋ BUR を連れ回す ＋ 慣性» の 4 項で閉じる。**BUR は歯車で駆動されず**ワークロールとの摩擦で連れ回されるので、上下を別々に stick/slip で解く。逆転の勾配は «BUR を滑らせない上限»（実測 47.8 mpm/s）で頭打ちになる —— 主機の能力（110 mpm/s）より先に効く |
| 加工発熱 | `ΔT = η·1.155·kf·ln(hIn/hOut)/(ρ·cp)`（η=0.95） |
| 巻取張力 | `T = min(M_reel/(OD/2), 14,480 kgf)`。リール駆動トルク `M_reel` は 550 kW / 300 rpm / 8.5:1 から求める |
| 前方張力 | 巻取パスでは `kf` を `T/(w·h)` ぶん低下させる（上限 30 MPa） |
| ロール抜熱 | `ΔT = (T−T_roll)(1 − exp(−2·h_c·t_c/(ρ·cp·h)))`、h_c = 30 kW/m²K。指数形で飽和させ、両表面から浸透深さ δ = 2√(α·t_c) の層に配分する |
| 表面放熱 | 放射（ε=0.18）＋自然対流（8 W/m²K）。板厚に反比例して効く |
| クーラント冷却 | 上面 800 / 下面 250 W/m²K（膜沸騰域）、液温 40 ℃、冷却域 2.4 m を通過する時間だけ効く |
| 板面冷却 | 水冷 700 W/m²K（受け手＝蒸気膜 105 ℃）／ 空冷 120 W/m²K（受け手＝周囲 30 ℃）／ なし 0。`PhysicsEngine.stripFilm()` が 1 か所で決める |
| 抜熱の飽和 | **すべての抜熱項**を `(T − T_sink)(1 − exp(−x))` の形で書き、合計も «いちばん冷たい受け手» で止める（`Rolling.passTempChange`）|
| 板厚方向の温度 | 9 層の 1 次元熱伝導を陰解法（三重対角）で毎ステップ解く |
| 反り | `κ = 0.04·(kf_top − kf_bot)/kf·ln(h_in/h_out)/h_out`、浮き上がり長 `ℓ = √(2·EI·κ/q)`、先端 `κℓ²/4` |
| 組織の履歴 | 亜結晶粒径 `1/d = A + B·log₁₀Z`（変形中はその場の Z だけで決まり履歴に依らない）、静的再結晶 `t₅₀ = A·ε⁻²·exp(Q/RT)`・`X = 1−exp(−0.693·(t/t₅₀)^N)`（Avrami）、残留ひずみ `ε_ret = ε_pass·(1−X)`。**まだ荷重へは返していません**（0-1 の 3） |
| コイル外径 | `OD = √(ID² + 4·L·h/π)`（内径 Φ610 から成長、最大 Φ1,900） |

可逆圧延で材料温度を決める主因はロール抜熱です。線形式は低速・厚板で発散するため
指数形（飽和する集中定数解）を使っています。

### 板厚方向の温度分布と上面の偏冷

板は上面から下面まで **9 層** に分け、1 次元の熱伝導を陰解法で解きます。ロール抜熱は
両表面の浸透深さ（2√(α·t_c)、厚板で 10 mm 程度）に集中し、そのあと伝導で均されます。
クーラントは水主体のエマルジョンで、上ロールから **板の上面へ流れ落ちる** ぶんが板を直接
冷やし、下面は流れ落ちないので効きが弱い（上面 800 / 下面 250 W/m²K。400 ℃ 級の面では
膜沸騰になるので 1 kW/m²K 未満に留めています）。冷却域はミル前後 2.4 m で、板がそこを
通過する時間だけ効くため、板全体の平均としては 2.4 m ÷ 板長 に薄まります。

**板面クーラント**（門型 2 基 × ラインに平行なヘッダ OS / DS 各 1 本、冷却域 1,200 mm × 2）は
板の上面へ直接噴射するので、ロールから流れ落ちるぶんとは別に上面へ 700 W/m²K で効きます。
ノズルは**板のクォーター部（ライン中心から ±620 mm）を狙って鉛直から 34° 内向き**に傾けてあります
（`Layout.nozzleDir()`。ヘッダの形状と噴射粒子の向きが同じ式）。
門型は**入側・出側とも「サイドガイドの上」（ミル中心から ±3.4 m）**にあり、
位置は `CONFIG.TABLE.GUIDE.HEADER.STATIONS` に書いて `Layout.coolStations` が解き、
冷却域は `Layout.coolZones()` が返します（描画・熱計算・噴射粒子・諸元表が同じ 1 式を見ます）。板がヘッダの下を
通過している長さ ÷ 板長 の割合（`PhysicsEngine.guideCoolFrac`）で薄めるため、短い厚板ほど強く、
長い薄板ほど平均としては弱く効きます。スケジュール予測も同じ冷却域を含み、
**パススケジュール区分の「板面冷却」（水冷 / 空冷 / なし）**で運転・予測とも切り替わります
（既定は水冷。1 パスあたりの抜熱は 水冷 16.3 K ＞ 空冷 14.1 K ＞ なし 13.5 K）。

その結果、厚板パスでは **上面が下面より 10〜20 ℃ 冷え、芯は 50〜100 ℃ 高い** 分布になり、
変形抵抗は層ごとの平均で見るので同じ平均温度でも荷重が上がります。薄板は伝導で瞬時に均され、
分布はほぼ消えます（«特に厚みが大きいとき» に効く、という実機の感覚と一致）。
モデルの単体検証は `tools/thermcheck.mjs`（放熱速度・抜熱のエネルギー収支）で行っています。

### 反り（上反りと自重たわみ）

上下面の変形抵抗差で上下の伸びが違い、板は硬い側（冷えた上面）へ曲がって **上反り** になります。
曲率 κ は圧延中に通過した分の重み付き平均で決まり、先端側（圧延済み）は新しい反り、
尾端側（未圧延）は前パスの反りを持ちます。

反りは自重と釣り合います。曲げ剛性 `EI = E·w·h³/12`、単位長さ重量 `q = ρ·g·h·w` として

- **テーブル上の端部**は `ℓ = √(2·EI·κ/q)` だけ浮き上がり、先端高さは `κℓ²/4`（スキー）
- **ロールから出たばかりの先端**は片持ち梁で、`κx²/2` の上反りから自重で垂れ、
  自由長が `√2·ℓ` に達するとテーブルに着地する。以後は出口側の形を保ったまま、
  先端にスキーが育つ

先端高さは `EI·κ²/(2q) = E·h²κ²/(24ρg)` で、同じ相対温度差なら板厚によらず同程度です。
厚板は温度差が残るので反りが出て（A7075・60 mm 板の中間パスで先端 230〜265 mm、R ≈ 70〜130 m）、
薄板は温度差が消えるので平坦になります。純アルミ（A1050）は温度依存が小さいので
同じ温度差でも反りは小さくなります（8 mm コイルの粗パスで最大 50 mm）。
数値は `tools/curltrace.mjs` でパスごとに読めます。

板長・板厚は**質量流一定**（出側速度 = ロール周速、入側速度 = 出側速度 × hOut/hIn）で積分するため、
体積が原理的に保存されます。最終体積の差 −2.15 % は数値誤差ではなく、**75 mm シャーが切り落とした端材ぶん**です（端材 800 mm のうち板長として減る健全部 687 mm）。

巻取張力は定数ではなく、**リール駆動トルクとコイル径から導いています**。
リール電動機は 550 kW・基底 300 rpm・減速比 8.5:1 なので、軸トルクは
`550 kW ÷ (300 rpm) × 8.5 ≒ 148.8 kN·m`。張力は `トルク ÷ コイル半径` で、
巻き太るほど下がります。

ここで得られる数値が仕様と整合します。最大コイル径 Φ1,900 でも `148.8 kN·m ÷ 0.95 m ≒ 16.0 ton` で、
仕様の巻取テンション最大 **14,480 kg を上回ります**。つまりこのリールは
「使用コイル径の全域で定格張力を出し切れる」ように電動機が選定されており、
モデル上も張力は径によらず 14,480 kgf で頭打ち（トルク律速に入るのは Φ2,096 以上）になります。
仕様値どうしが独立に整合したので、駆動側の数値の読み方が正しいことの裏付けになっています。

### 材料温度の下限は工程と材質で変わる

粗〜中間パスは熱間域を保つ必要がありますが、**薄板をコイルに仕上げる最終パス以降は
巻取温度まで下がるのが正常**です。下限は材質表から `熱間域下限 − 30 ℃`（圧延中）と
`巻取温度 − 30 ℃`（コイル仕上げ・巻取）を工程で切り替えています。
巻取中は温度低下そのものが工程なので、警告判定の対象から外しています。

### パススケジュールの自動生成

パス数と圧下量は板厚・板幅・材質から `Rolling.buildSchedule()` が生成します。目標板厚は
4〜150 mm で自由に設定でき、10 mm 以下はコイル、10 mm 超は板材として扱います。
1パスあたりの圧下量は **噛み込み角（24°）・圧下率（40 %）・圧延荷重（常用最大の 88 %）** の
3つで律速し、速度は質量流に合わせて上げたうえで **主機の能力**で頭打ちにします ——
上限は実機のトルクカーブそのもの（定格 4,000 kW × **過負荷耐量 225 %**）で、
以前置いていた推定の margin（0.84 / 0.95）は使いません。
温度は選んだ速度に依存する（低速ほどロールに熱を奪われる）ため、速度→温度→速度 を
反復して収束させています。可逆圧延なので最終材が出側に残るようパス数は奇数にそろえます。

パスは次の3つで律速します。

1. **噛み込み角 24°** — 前段の厚板パスはこれで圧下量が決まる
2. **テーブル長** — 中間パスは終わる側のテーブルに板が載りきる必要がある
3. **主駆動系の包絡線** — 後段の低温パスは所要トルクで速度が決まる（定トルク／定出力）

さらに「各パスを所定時間（150 秒）内に通し切れるか」も条件に入れています。長いパスほど
速く通す必要があり、そこで定格動力を超えるなら圧下量を落とします。温度は速度に依存するので
速度→温度→速度を反復して収束させます。

荷重の上限はパスの «終わり» の温度で見ます。主機は動力律速で低温パスほど遅くなり、
遅いほど通過中に冷えて硬くなるので、入側温度と参照速度で組むと薄板の後段（特に巻取パス）で
実荷重が上限を超えます。さらに運転時は表層が冷えて変形抵抗が層平均で上がるため、
集中定数の予測より実荷重が 2〜3 割高くなります。スケジュールは常用最大の 70 % で組み、
実荷重が常用最大 3,920 t を超えないようにしています。

**実測**（既定ロット ＝ 実機の較正ロット。A5052・呼び厚 560 − 片面 15 面削 ＝ 530 mm、
幅 1,330 mm、長さ 3,450 mm、炉出し 433 ℃、目標 8 mm コイル）。
数字は `node tools/calib.mjs` と `node tools/realsched.mjs` の出力です:

| 指標 | 本アプリ | 実機 | 判定 |
|---|---|---|---|
| パス数 | **23**（可逆・最終パスで巻取） | 23 | 板厚・速度とも一致 |
| 最大圧延荷重 | **3,268 t**（常用最大 3,920 t の 83 %） | 巻取パス 2,400〜2,900 t | 平均 2,427 t で帯の中 |
| 上がり温度 | **351 ℃**（頭 396 → 尻 302、コイル平均 341） | 360 ℃ ±15 | 帯の中 |
| 8 mm 完了時の板長 | **215.4 m**（尻の板厚 7.9 mm） | 219.4 m ±3 % | 帯の中 |
| 所要時間（厚板段 ＋ 仕上げ） | **514 s**（358 ＋ 156） | 450 s ±25 % | 帯の中 |
| 所要時間（26→16 ／ 16→8 巻取） | **89 s ／ 240 s** | 90 s ／ 240 s | 一致 |
| 1 ロットの所要 | **846 s**（14.1 分） | — | — |
| 体積収支 | **板 ＋ 切り落とし ＝ +0.8 %** | — | 保存されている |

予測（スケジュール生成）と運転（物理）の食い違いも毎回見ています ——
**定常荷重は全パスで ±15 % 以内（最大差 3.9 %）、最大荷重は ±20 % 以内（同 16.5 %）、
温度収支は各パス 10 K 以内（同 −5.3 K）**。`calib` 14/14。

重圧下パスは低速、薄板パスは高速という実機どおりの姿になります。
主機はトルクカーブを超えるトルクを出せないため、運転時にも自動的に減速します。
既定ロット（A5052）では巻取パス（16 → 8 mm）が最も重く、**最大荷重 3,268 t ＝ 常用最大の 83 %**。
板が薄く速度も低いので、ここだけ**スタンドの固有振動（15 Hz）が立ちます** —— 実機のチャタに
相当する現象で、制御の発振ではありません（0-4 の 23）。

### 成立しない条件は事前に警告し、運転すれば過負荷停止する

スケジュールには各パスの予測荷重（終端温度・動力律速速度・巻取張力で見た値に、厚板ほど残る
表層硬化ぶん 1.0〜1.25 を掛けたもの）と終端温度を持たせ、`Rolling.scheduleFeasibility()` が
**予測荷重が非常最大の 1.1 倍（予測の不確かさ ±10 % を見込む）を超える** か
**最終パスの終端温度が材質の下限を 30 ℃ 以上割る** 場合に «このラインでは成立しない» と判定して、
素材パネルに理由を出します。常用最大の 90 % を超える、または下限をわずかに割る場合は
«余裕なし» の注記になります。スケジュール表の荷重列は運転前は予測値（灰色）、運転中は実測の最大値です。
予測は保守側に 10〜20 % 程度で、A5052・8 mm では予測 4,231 t に対して実測 3,460 t でした。

それでも運転した場合、実機と同じく **非常最大圧延荷重 4,200 t を超えた時点で主機を止めて運転を
打ち切ります**（過負荷停止）。例えば A7075 を 8 mm コイルに仕上げようとすると、熱間域が
360〜450 ℃ と狭く薄板パスで温度が保てないため、動力律速 → 減速 → 冷却 → 硬化の悪循環に入り、
第 6 パスで過負荷停止します。これは «この材質・板厚はこのラインでは作れない» という
物理的な結論で、成立させるには炉出し温度・板厚・材質の見直しが要ります。

### 意図的に採用しなかった表現

アルミニウムの熱間圧延温度域（400–540 ℃）では材料は**可視発光しません**（赤熱は 600 ℃ 以上）。
そのため赤熱表現は行わず、温度は色味と表面粗さの微差、および水蒸気・クーラント演出で示しています。
クーラントも材料への直接噴射ではなく、実機同様ワークロール面への潤滑エマルジョン噴射としています。

### 水蒸気とクーラントの描画

粒子は 2 系統あり、どちらも 1 ドローコールの点スプライトです。

クーラントは **水を主成分とするエマルジョン（油分 6 wt% 程度）** です。したがって
ミル上に立ち上がる白煙の主成分は **水蒸気** であり、油煙ではありません。発生量は
«水がどれだけ蒸発できるか» で決まるので、材料温度に比例させています
（120 ℃ で発生し始め、480 ℃ で飽和）。仕上げの低温パスでは薄くなります。

| | 水蒸気 | クーラント液滴 |
|---|---|---|
| 発生源 | ロールバイト出側（エマルジョンの水が熱で蒸発） | ワークロール面（エマルジョン噴射） |
| 粒数（上限） | 300 | 800 |
| 初期径 → 末端径 | 620〜1,140 mm → 2.6 倍 | 70〜140 mm → 1.3 倍 |
| 鉛直加速度 | 浮力 +2,000 mm/s²、抗力 0.95 /s（終端上昇速度 2.1 m/s） | 重力 −9,810 mm/s² |
| 寿命 | 2.4〜4.2 s | 0.45〜0.80 s |
| 放出レート | (30 + 0.50 × ライン速度) × 温度係数 [1/s] | 620 /s |

点スプライトが「四角い塊」に見えるのは輪郭が矩形で切り落とされるためなので、
**中心から滑らかに消えるアルファ**を持つテクスチャを起動時に Canvas で生成して共有し
（画像ファイルは持ちません）、粒ごとに **向き・大きさ・濃さ・色** を変えています。
油煙用のテクスチャは単純な放射グラデーションではなく、小さな塊を重ねて輪郭を崩した
不定形のパフです。粒は寿命に沿って **膨らみながら薄れ**（体積が保たれれば濃度は下がる）、
立ち上がりも滑らかにして唐突な出現を避けています。

速度は `v' = 浮力 − 抗力·v` の**解析解**で積分しているため、フレームレートが変わっても
軌跡が変わりません。放出数も端数を次フレームへ繰り越す方式にして、dt 非依存にしています。
粒径が大きいぶん塗り面積が効くので、描画品質「軽量」では放出量を 40 % に落とします。

**粒は「小さく多く」ではなく「大きく少なく」**にしています。小さい粒を多数撒くと遠景で
«点の集合» に見えてしまい、塗り面積も増えるためです。濃度は目視ではなく、水蒸気
ON/OFF のキャンバス画素差を測って決めました。

---

## 2-2. 頭と尾で荷重が上がる理由（実機の話）

実機で «噛み込んだ瞬間» と «頭・尾» の荷重が高くなるのは、**単一の原因ではなく 6 つの要因の重ね合わせ**です。
本アプリが再現しているものと、していないものを分けて書きます。

| # | 要因 | 中身 | 本アプリ |
|---|---|---|---|
| 1 | **端部の温度低下** | 端面（幅×板厚）からも放熱するので単位体積あたりの放熱面積が大きい。炉内でも端は扉・炉壁に近く、抽出から圧延開始までの搬送でも端から冷える。さらに端は舌・ワニ口で薄く広がっており、薄い＝冷えやすい。可逆圧延では往復のたびに端がテーブル上で待つ。変形抵抗は温度に指数で効くので、**30 K の低下で 15〜20 % 硬くなる** | ○ 実装 |
| 2 | **張力が掛からない** | 前方・後方張力は圧下力を実質的に下げる。頭は巻取機に噛むまで、尾は尻抜けで張力が消えるので、**頭尾だけ «無張力圧延»** になる。実務でいちばん効く要因 | △ 巻取パスのみ |
| 3 | **前パスが残した板厚偏差** | 前のパスの頭尾も同じ理由で公称から外れる（AGC が収束していない・無張力で厚め）。次のパスではその厚い部分が入るので圧下量が増える。**パスを重ねるほど累積する** | ○ 実装（材料座標のプロファイル） |
| 4 | **板厚制御の過渡** | ゲージメータ AGC はミル定数の推定誤差ぶん残留偏差を持ち、X 線厚み計は出側に離れているので**頭が計器に届くまで補正が入らない**（輸送遅れ）。圧下自身にも応答遅れがある。つまり頭の数 m は実質「開ループ」 | ○ 実装 |
| 5 | **噛み込みの衝撃** | 圧下はミルの伸び `F/M` を見込んで締めてあるので、荷重が立つまでの数十 ms は**狭い無負荷ギャップがそのまま材料に当たる**。加えてスタンドの質量とばね（固有振動数 10〜30 Hz）が階段状の荷重で励振され、チョック・圧下ねじの «がた» が荷重で詰まるまで実ギャップは設定より狭い。頭はクロップで直角に切られており、噛み込み条件も定常より厳しい | ○ 実装（スタンドの伸びは 2 次系 15 Hz・ζ 0.35。ロードセル値＝伸び × ミル定数の行き過ぎが衝撃として出る。がたは未実装） |
| 6 | **潤滑膜が未形成** | ロールバイトの潤滑膜は材料が入って初めてできる。頭は乾いた状態で噛むので**摩擦係数が高く、摩擦丘が大きくなる**（面圧が上がる） | ○ 実装（`PROCESS.MU_HEAD`: 頭で μ +25 %、1 m で膜が育つ。係数は文献的な目安） |

尾端にはさらに «尻跳ね»（tail flick）で板が暴れ、瞬間的な片当たり荷重が出るという固有の現象もあります。

## 2-3. 精度を上げるために «まだ入れていない» 理論

> **荷重まわりのロードマップは `docs/圧延荷重に効く要因.md` の §6 に移しました。**
> 同じ表が 2 か所にあると、片方だけ直して食い違います。あちらは**実装を実際に測った数字**で
> 優先度を付けてあり（★★★ 残留ひずみを変形抵抗へ返す／接触弧内の温度降下／混合潤滑）、
> 必要な実機データも項目ごとに書いてあります。
>
> ここに残すのは、**荷重ではなく «形状・板厚・制御» に効く**もの —— あちらが扱わない側です。

### いま残っているもの（形状・板厚・制御）

| # | 項目 | いま何が起きているか / 入れると何が変わるか | 効き |
|---|---|---|---|
| 1 | **界面熱伝達係数の圧力依存** | `h_c` は接触圧力・酸化膜・潤滑膜で 2〜10 倍変わる。いまは界面 25 kW/m²K 一定を両側の内部抵抗と直列にした実効値（接触時間で 12〜15 kW/m²K）。抜熱が変われば表層温度が変わり、**上下差＝反りの駆動力**が直接変わる | 表層温度 ±20 K・反りの駆動力 |
| 2 | **サーマルクラウンを軸方向の熱伝導で解く** | いまは «一様に膨らんで飽和する» 形。実際はロール胴が軸方向に温度分布を持ち、板幅より外は冷えたままなので、**板幅が変わったとき前の幅の跡（ボディマーク）が残る**。ロット替えの形状不良がそのまま出せる | 形状（幅替え時） |
| 3 | **幅広がりを質量流へ戻す** | いまは幅広がりを «結果» として持つだけで、伸びの計算には入れていない。体積保存の残差 0.25 % はほぼこれ | 板長 0.25 % |
| 4 | **キャンバーを材料座標で持つ** | いまはスカラーの曲率。長手で曲率が変わると板は **S 字**に曲がるが、それが表せない | 蛇行の形 |
| 5 | **偏芯補償（ノッチフィルタ）** | 偏芯そのものは入っています（`ECC_BR` / `ECC_WR`）。**無いのは実機の AGC が持つ補償**で、長手の板厚の振れが実力より 12 % 高い理由として残っている唯一の説明です。切れば 3σ 44 µm でちょうど実力に入ることまで測ってあります（→ 0-1 と 0-4） | 板厚の振れ −23 % |
| 6 | **マスフロー AGC / 張力 AGC** | 入出側の速度計から板厚を推定する（X 線より速い）／圧下ではなく速度で厚みを合わせる | 頭部オフゲージ長 |
| 7 | **油圧圧下の 2 次系** | いまは一次遅れ。実際は油柱の圧縮性を含む 2 次系で、**噛み込みの衝撃で振れる** | 噛み込み時の板厚 |
| 8 | **幅反りの «冷却差» 経路** | いまの幅反りは長手の反りからのアンチクラスティック（ポアソン効果）だけ。実機にはもう 1 つ「**上下面の冷却差が幅方向で違う**」経路がある —— 下面はテーブルローラの**両エッジ寄り 2 本の接触線**でだけ冷えるので、エッジ寄りだけ上下の温度差が変わる。層モデルが幅方向を解いていないので未実装 | 幅反りの大きさと向き |
| 9 | **せん断帯（加工発熱の板厚分布）** | 加工発熱は板厚に一様ではなく、Δ ＝ h̄/Ld が大きいパスでは表層のせん断帯に集中する。**ワニ口の駆動力そのもの** | 端部欠陥の出方 |

## 2-4. 板の «形状・反り・歪み» —— いま入っているもの / 足りないもの

形状の精度は「①**断面（幅方向）の板厚分布**をどう決めるか」「②**長手の曲がり（反り）**を
どう解くか」「③**残留応力と平坦度**をどう結びつけるか」の 3 つで決まります。
**3 つとも力学として入っています** —— ① と ③ は影響係数法（下の «幅方向の形状»）、
② は曲げの釣り合い（次の表）です。

### いま «力学として» 入っているもの

| 項目 | 実装 |
|---|---|
| 上下非対称による反り（曲率） | `SlabState.curl` —— 上下ロール周速差・上下温度差・摩擦差から曲率 κ を積む |
| 自重と曲げ剛性の釣り合い | `Rolling.curlSpan(κ, h, w, alloy)` —— 曲げ剛性 `EI = E·w·h³/12`、自重の分布荷重 `q = ρ·g·h·w` を出し、**浮き上がり長 `ℓ = √(2·(EI/q)·κ)`**、先端高さ `κℓ²/4`、着地距離 `√2·ℓ` を決める |
| ロールから出た片持ちのたわみ | `Rolling.cantileverLift` —— 曲率による持ち上がりから**自重による垂れ `−x²(6ℓ²−4ℓx+x²)/(24·EI/q)`** を引く |
| テーブル上のスキー（浮き上がり） | `Rolling.skiLift` —— 端が浮き、根元でテーブルに接する形 |
| 端部欠陥（舌・ワニ口） | `SlabState.overhangParts` —— 舌は幅方向の不均一な広がり、ワニ口は表層集中変形（Δ = hMean/Ld > 1） |
| 長手の板厚プロファイル | `hProf`（材料座標 240 分割）—— 各パスが «出たときの実ギャップ» を書き戻す |
| 断面の鋳肌ラウンド | `Sect` —— 鋳造ままのコーナー半径が面削で頂部を削られ、圧延で自己相似に縮む |

**したがって「自重・たわみは考慮されているか」への答えは «曲げの釣り合いとしては入っている» です。**
反った端がどこまで浮くか（ℓ）も、先端がどこで着地するか（√2·ℓ）も、片持ちがどれだけ垂れるかも、
すべて `EI/q` ＝ 曲げ剛性 ÷ 自重 から出しています。板厚が薄いほど `EI ∝ h³` が効いて浮き上がり長が
短くなる、という «実機の見え方» はここから自然に出ます。

### 幅方向の形状 —— ロールたわみと板クラウン（影響係数法）

ロール系のたわみは **影響係数法** で解いています。WR は自由端梁、BUR は軸受間（3,600 mm）の
単純支持梁、両者は **ヘルツ線接触でつぶれる**（2.85 t/mm per mm）という連立を

```
  y_WR − y_BUR = q/k          接触のつぶれ
  y_WR = A·(p − q) + y_b      WR（自由端梁。p: 板荷重、q: BUR からの反力、y_b: ベンディング）
  y_BUR = B·q                 BUR（軸受間の単純支持梁）
⇒ (A + B + I/k)·q + c·1 + t·z = A·p + y_b     （c, t は剛体運動）
  ∫q = ∫p + 2F_b,  ∫q·z = ∫p·z                 （WR の力とモーメントの釣り合い）
```

として (n+2)×(n+2) で解き、ここから «板荷重 → ギャップ» の影響係数 G と
«ベンディング → ギャップ» の Hb を作ります。板の側は塑性係数
`Q_w = −dP′/dh_out`（`Rolling.solve` の数値微分）で閉じるので、全体は

```
  (I + Q_w·G)·x = G·p̄ + H_b·F_b + crown
```

という線形方程式 1 本になります（x は零平均の板厚偏差）。

**平坦度は «クラウン率 C/h の変化» で決まります。** 入側と出側で C/h が同じなら、
どれだけクラウンが付いていても伸びは幅方向に一様＝平坦です（比例クラウンの原則）。
崩れたぶんが伸び差 λ になり、板は 1 枚なので長さが揃わねばならず、差が
残留応力 `σ = −E(λ − λ̄)` と I-unit `(λ − λ̄)×10⁵` になります。

実測例（A5052・スラブ 536 mm・幅 1,500・11 パス）。
**いまの既定ロット（530 × 1,330 × 3,450・23 パス）とは別の条件**なので、
現在値は `node tools/shapetrace.mjs` で取り直してください:

| パス | 出厚 mm | 荷重 t | クラウン µm | クラウン率 % | エッジドロップ µm | I-unit | 判定 | ベンダ t |
|---|---|---|---|---|---|---|---|---|
| 1 | 455.4 | 1,433 | −24.6 | −0.005 | −6.4 |  5.4 | 平坦 |   0 |
| 4 | 212.9 | 2,036 |  −8.7 | −0.004 | −4.5 |  2.1 | 平坦 |  12 |
| 6 |  75.3 | 2,756 |  −0.6 | −0.001 | −5.2 |  9.9 | 中伸び |  49 |
| 8 |  31.7 | 2,820 |  −1.7 | −0.005 | −6.3 | 22.6 | 中伸び |  71 |
| 9 |  22.5 | 3,035 |  13.2 |  0.059 | −3.6 | 68.6 | 耳伸び |  68 |
| 11 | 12.9 | 2,293 |   7.7 |  0.059 | −2.1 | 16.5 | 中伸び |  20 |

幅は **1,500 → 1,509.5 mm**（幅広がり 9.5 mm）。端の冷えは 7.5〜12 K。
形状制御（比例クラウン制御）はベンディングのクラウン利得 **−0.69 µm/t** から
必要な曲げ力を割り算 1 回で出しています。

### 最終パスの尻の荷重 —— 温度が支配的

実機スケジュールの最終パス（16 → 8 mm、幅 1,330、50 mpm、A5052）は **尻がどこまで冷えるか**で
荷重が決まります。較正前は尻が 246 ℃ まで冷えて 4,231 t（非常最大 4,200 t 超）で止まっていました。
実機の荷重帯（2,400〜2,900 t）と上がり温度（360 ℃）を渡されて項別に検算したところ、冷え過ぎの内訳は
次のとおりでした（`tools/calib.mjs` の温度収支）:

| 項 | 較正前 | 較正後 | 直したこと |
|---|---|---|---|
| ロール抜熱 | 51 K | 29 K | 界面の熱伝達率だけで書いていた。ロール・板の内部へ熱が染み込む抵抗（半無限体 √(πt)/2e）を直列に（`Rolling.rollHTC`） |
| 放射・対流＋テーブル接触（巻取パス） | 60 K | 32 K | 巻かれた部分に放熱面は無い。露出しているのは入側で待つあいだだけ（入側厚で半分の時間） |
| クーラント（ロール周り・出側ヘッダ） | 33 K | 26 K | 膜沸騰の熱伝達は液のサブクール度で動く（64 ℃ で 0.79 倍） |
| 端部の薄さの割増 | あり | クロップ後は無し | クロップ後の端は角が立っていて薄くない |

結果、最終パスは **頭 2,338 → 尻 3,687 t（平均 2,623 t）**、出側板温 350 ℃、尻の局所温度 300 ℃。
尻が頭より 1.6 倍重いのは «4 分待つあいだに 16 mm の板が放射・対流で 45 K、テーブル接触で 10 K 冷える»
という避けようのない物理で、実機の帯（頭 2,400 → 尻 2,900）が示す向きと同じです。
予測側もこれに合わせ、巻取パスの峰は «パス時間いっぱい待った尻» の温度で解いて
非常最大に余裕を見て当てます（`Rolling.peakForce` / `PROCESS.TAIL_K`）—— 常用最大に収めようとすると、
実機が通している 16 → 8 mm が 8.8 mm で止まります。

### 反りの «残り方» —— スプリングバック

バイトの中で付いた曲率がそのまま形になるわけではありません。出口で荷重が抜けると
弾性のぶんだけ戻り、残るのは塑性変形した分だけです。断面を弾完全塑性の長方形と見ると
モーメント–曲率が厳密に書けます。

```
  κ_e = 2σ_y/(E·h)                  表面が降伏し始める曲率
  M   = M_p·(1 − (1/3)(κ_e/κ)²)     κ ≥ κ_e（M_p = σ_y·b·h²/4）
  κ_perm = κ − M/(E·I)              除荷は弾性なので、この曲率だけ戻る
```

`κ ≤ κ_e` なら断面はまだ弾性なので**すべて戻って形は残りません**。実測（既定ロット）:

| パス | 出厚 mm | 上下 ΔT | バイト内 半径 m | 残る反り 半径 m | 弾性限界 半径 m | κ/κ_e |
|---|---|---|---|---|---|---|
| 1 | 455.4 | 12.1 | — | **平ら（弾性で戻る）** | 276.8 | 0.8 |
| 2 | 374.6 | 21.8 | 124.9 | 1,875.6 | 208.5 | 1.67 |
| 4 | 213.0 | 30.6 | 30.1 | **78.2** | 99.6 | 3.31 |
| 5 | 132.2 | 26.9 | 17.9 | **32.5** | 54.9 | 3.07 |
| 6 | 75.3 | 11.0 | 23.4 | 75.9 | 24.6 | 1.05 |
| 7〜11 | 47.9〜12.0 | 3.1〜0.0 | — | **平ら（弾性で戻る）** | 12.1〜1.3 | < 1 |

**残る反りを作っているのは中盤の厚いパスだけ**で、薄いパスは弾性で戻ります ——
実機で「厚板パスで頭が跳ね上がり、薄板パスは平らに走る」のと同じ向きです。

### 蛇行 —— キャンバーと差荷重制御

```
  ロールギャップの左右差（くさび）→ 両耳の伸びの差 → 平面内の曲がり → 横行
                    ↑                                              │
                    └────── 荷重の合力が中心から外れる ←───────────┘
```

くさびは**ミルのゼロ点誤差**（0.02 mm）と、**板がミル中心からずれることで
荷重の合力が外れてロール系が傾くぶん**から出ます。後者があるため
**正の帰還**になり、放っておくと発散します —— これが実機で蛇行が
一度始まると止まらない理由です。断ち切るのが**差荷重制御（自動レベリング）**で、
OS / DS の荷重差が出ないように圧下を傾けます。実測:

| | 差荷重制御なし | **あり** |
|---|---|---|
| 平均キャンバー | 6.09×10⁻⁷ 1/mm | **3.42×10⁻⁹ 1/mm**（178 分の 1）|
| サイドガイド接触 | 288.3 s | **1.9 s** |
| 最大の横ずれ | — | 10.0 mm（ガイドの余裕 10 mm）|
| 圧下の傾き | — | 最大 0.025 mm（範囲 ±0.5）|

### 板反りの «対策» とその効果

実機で使う 2 つの手を運転から操作でき、効果が数字で出ます。

**① パスライン調整（WR）** —— ロールの組をテーブルに対して上下させる。
バイトを出た板は最初のテーブルローラ（750 mm）まで δ だけ戻らねばならないので、
根元に `κ_pl = −3δ/L²` の曲げが入る。スプリングバックと組で効くので、
**小さいうちは除荷で戻って反りは変わらず、ある程度動かすと効き始める**。

| パスライン | \|κ\| 1/mm | 上反り κ | 下反り κ | 端の浮き mm |
|---|---|---|---|---|
| −20 mm | 1.38×10⁻⁴ | 1.38×10⁻⁴ | 0 | 24,684 |
| −10 mm | 8.53×10⁻⁵ | 8.53×10⁻⁵ | 0 | 17,190 |
| 0 mm（基準）| 3.26×10⁻⁵ | 3.26×10⁻⁵ | 0 | 1,472 |
| **+5 mm** | **2.07×10⁻⁵** | 8.77×10⁻⁶ | −2.07×10⁻⁵ | **106 ← 最適** |
| +10 mm | 4.74×10⁻⁵ | 0 | −4.74×10⁻⁵ | 0（下反りへ）|
| +30 mm | 1.54×10⁻⁴ | 0 | −1.54×10⁻⁴ | 0（打ち消し過ぎ）|

**② 板面冷却の入側／出側 ON/OFF** —— クーラントは**板の上面にだけ**掛かるので、
片側を切ると上下の温度差が変わり、変形抵抗の差（＝反りの駆動力）が変わる。

| 設定 | 端の浮き | 上下 ΔT |
|---|---|---|
| 両方 ON（基準）| 1,425 mm | 30.7 K |
| 入側 OFF | 321 mm | 24.5 K |
| 両方 OFF | 29 mm | 16.5 K |

熱計算・スケジュール予測・噴射の描画・諸元表がすべて同じ 1 式
（`Layout.coolHeaderXs`）を見るので、切った瞬間から予測も描画も追従します。


## 2-5. 文献から取り出した «組織» のデータ（ご提供いただいた 5 報）

ソーキング条件・上下温度差・組織が荷重や板形状に効くか、という問いに対して
ご提供いただいた文献から取り出せたものを、使える形にして残しておきます。

### A. 土公・浅見・戸次・松本「工業用アルミニウムの多パス熱間圧延時の組織変化」軽金属 41 (1991) 534

**本アプリのモデルにいちばん近い実験**です（1100 の DC 鋳塊・4 種の均質化処理 →
Φ430 ロール・40 mpm で 2〜8 パスの多パス熱間圧延）。取り出せる結論は 3 つ。

| # | 結論 | 本アプリへの意味 |
|---|---|---|
| 1 | 熱間圧延中に形成される**亜結晶粒径は直前のパスの Z 因子だけで決まる**（`d⁻¹ = a + b·log Z`）。それ以前のパスの蓄積ひずみ・析出相分布・固溶量の影響を受けない。理由は熱間圧延が «ひずみの導入と動的回復が釣り合った定常変形» だから | **現行モデルの正当性の裏付け。** 本アプリは Sellars–Tegart 則でその場の Z から変形抵抗を決めており、組織の履歴を持っていない。文献は「硬さの主因（亜結晶粒径）が Z だけで決まる」と言っているので、**履歴項を足す必要がない** |
| 2 | **1100 では均質化処理条件（500 / 560 / 600 ℃×10h / 600→500 ℃）を変えても亜結晶粒径は変わらない。** 500〜600 ℃ 間で電気比抵抗が 0.6 nΩm しか動かない（＝固溶量差が小さい）ため | 1000 系ではソーキング条件は荷重にほぼ効かない |
| 3 | **3003 では変わる**（同誌の別報）。同じ温度範囲で比抵抗が **5 nΩm**（1100 の 8 倍）動く | Mn・Cr を含む材質では効く。固溶 Mn が多い → 動的回復が抑えられる → 亜結晶粒が細かい → 変形抵抗が高い |

そのほか: 析出は**パス間の保持中より圧延加工中に動的に**生じ、圧延温度が低いほど析出量が多い。
硬さは**亜結晶粒径と再結晶率でほぼ決まる**。

**注意**: `d⁻¹ = a + b·log Z` の a, b は Fig. 4 のグラフにしかなく、本文に数値がありません。
実装するなら文献の目安（log Z ＝ 10 で d ≈ 10 µm、14 で d ≈ 2 µm）を置いて «目安» と明記することになります。

**上下温度差との関係は定量できます。** 亜結晶粒径も変形抵抗も Z を通して効くので

    Δln Z = Q·ΔT / (R·T²)

A1100（Q ＝ 158 kJ/mol）・板温 480 ℃ で、上下 50 K の差は **Z を 5.3 倍**（変形抵抗の比 1.45、m ＝ 0.22）、
持ちかかり時の 14 K でも **1.6 倍**（変形抵抗の比 1.11）に相当します。**この経路は本アプリにすでに入っています**
（層ごとの温度から変形抵抗を出し、上下差が反りの駆動力になる）。

### B. 池野・柴田・慶寺「工業用純アルミニウムの圧延工程における組織的変化について」軽金属 (1958)

2S スラブ 120 mm → 7 mm（ソリュブルオイル冷却つき）の板厚方向の不均一。

| 観測 | 数値 |
|---|---|
| 熱間圧延材は**表層が軟らかく内部が硬い**（表層が再結晶するため） | — |
| 表層が再結晶する加工度 | **55〜90 %** |
| 全面再結晶する加工度 | **72.8 %** 付近 |
| 再結晶せず圧延組織になる加工度 | **90 % 超** |
| 表層の再結晶粒径 | 板厚が薄くなるほど細かい（log 粒度 vs 板厚が直線） |

**「水冷しながら圧延してもこうなる」**ことから、著者は「ロールとの摩擦熱による熱的影響が極めて大きい」と
結論しています。本アプリは加工発熱を板厚方向に一様に配っており（`HEAT_EFF`）、**界面のせん断摩擦発熱を
表層に集中させていません**（2-3 の «せん断帯» が未実装であることの裏付け）。表層に集中させると、
いまの «表層は冷えて硬い» 一方向の効果に «表層は摩擦で温まる» 逆向きの効果が加わり、反りの計算が変わります。

### C. 財満・多々・松下「アルミニウム圧延板の方向性におよぼすソーキングの影響について」軽金属 (1962)

| 条件 | 結果 |
|---|---|
| ソーキング **360 ℃ vs 490 ℃** | 耳率（方向性）が明確に変わる。高温ソーキング＋高加工度で 45° 耳、低温＋低加工度で 0°/90° 耳 |
| ソーキング **500 / 510 / 520 ℃ × 2 / 5 / 7 h**（同著者の先行研究） | **顕著な差なし** |
| 熱間圧延温度（畑・竹内、2S） | **500〜550 ℃ では耳率にほとんど差がない。475 ℃・450 ℃ では差が大きく** 0°/90° 耳が優位になる |

**実機のソーキング温度域（おおむね 500〜560 ℃）では耳率への影響は小さく、効くのはむしろ
«圧延終了温度が 475 ℃ を割るかどうか»** —— こちらは本アプリが出せる量です。

### D. 三木「アルミニウム合金中の遷移金属元素 (1)(2)」軽金属 24 (1974)

定量データは少ないですが、A・B の解釈を支える記述が取れます。

- **固溶 Fe は微量でも再結晶温度を大幅に上げる。粗大に析出していると逆に下げ、再結晶粒を細かくする**
- **Mn の微細析出物は熱間加工性（押出性）を悪くする**
- **冷間加工により遷移元素の析出が著しく促進される**（加工誘起析出）—— A の «析出は圧延加工中に動的に生じる» と一致
- Al-1.3 % Mn は **550 ℃ 付近で析出速度が最大**

### この 5 報から «入れたもの» と «まだデータが要るもの»

| 入れたもの | どう入れたか |
|---|---|
| 亜結晶粒径 | `1/d = A + B·log₁₀Z`。文献どおり**変形中はその場の Z だけで決まり、履歴に依りません**。実測: 430 ℃/0.8 s⁻¹ で 3.4 µm、390 ℃/9.5 s⁻¹ で 2.1 µm（アルミ熱間の実測レンジ 1〜10 µm の中） |
| 静的再結晶（パス間軟化） | Avrami `t₅₀ = A·ε⁻²·exp(Q/RT)`、`X = 1−exp(−0.693·(t/t₅₀)^N)`。実測: 半減時間 450 ℃ で 2.0 s、390 ℃ で 29.7 s |
| 残留ひずみ（次のパスへの持ち越し） | `ε_ret = ε_pass·(1−X)`。実測: 厚板段で 0.13 前後、最終パスで 0.77（低温・短いパス間で再結晶しきれない） |

いずれも `PhysicsEngine._micro` で状態量として持ち、計器にも出しています（`tools/microtrace.mjs`）。
**係数はアルミ熱間加工の一般的な形と桁で置いた推定値**で、この材料・この圧延機で測った値では
ありません（0-2 の 11）。

| まだデータが要るもの | 必要なもの |
|---|---|
| **残留ひずみを変形抵抗へ返す** | 同じ材質でパス間時間を変えたときの荷重実績 2 点（→ 0-1 の 3）。**いま最優先** |
| ソーキング感受性の定量 | 3003 など Mn 含有材で、均質化条件を変えたときの荷重実績（1100 は文献どおり «効かない» で確定） |
| 表層摩擦発熱の較正 | 圧延直後の板厚方向硬さ分布、または表層の再結晶の有無 |

## 3. 構造

```
CONFIG          全寸法を mm 単位で保持する単一の真実（ALLOYS: 材質表）。3D 空間へは Sim.u() でのみ変換する
Num             時間積分ユーティリティ（approach / rateTo）。dt 非依存を構造的に保証する
Rolling         圧延理論（純関数）: 変形抵抗・荷重・温度・反り・コイル層・端部切断計画・仕上げ振分け
State           SlabState（層温度・反り）/ MillState / SupplyState / FinishState（巻き層・拡縮・払い出し）
Controller      MillController / SupplyController / FinishController（クロップ・巻取・払い出し）
PhysicsEngine   任意 dt を受け取り 1/120 s の固定ステップに分割して積分。板厚方向の熱伝導は陰解法
MAT / G / Parts マテリアル登録・ジオメトリ生成器・部材ライブラリ
CoilGeometry    段付きコイル（完成した層の環 + 巻きかけの扇形 + 段）を固定頂点数で更新する
View            MillStand / Drive / Table / Guide / Finish / Supply / Slab / Spray / Label
World3D         シーン・ライティング・カメラ・描画品質
UIManager       操作系・計器・状態バナー。待機中の素材変更は即時反映
```

（ファイルの並びは冒頭の表を参照）

描画と物理が同じ量を別々に計算しない、という原則を保っています。板の入口 A、下台の
持ち上げ量、反りの形、端部欠陥長、コイルの層と接点は、いずれも物理側の 1 か所を
描画側が参照します。

### 断面表示（どこを透かすかを選べる）

構造物が視界を塞ぐと «圧延の様子» が見えません。そこで各ビューは自分のメッシュを
**手前側（オペレータ側）／奥側（駆動側）／天面（板の上に架かる部材）** に分けて登録し、World3D は
その登録内容だけを見て透過を切り替えます。設備を追加しても切替ロジックは変わりません。

| モード | 効果 |
|---|---|
| 実体 | すべて不透明 |
| 半断面（既定） | 手前側と天面を透過。奥行きを保ったまま中が見える |
| 透視 | 手前・奥・天面とも透過 |

«天面» は 75 mm シャーのクロスヘッド・上刃ホルダ・プッシャレール、30 mm シャーのクロスヘッドと
シリンダ、板面クーラントの梁で、いずれも真上から板を隠す部材です。固定上刃・下刃（刃物色）、下台、
板、端材は «切れていることが見えるべき部材» なので透過しません。

さらに **どの設備に適用するか**（圧延機 / サイドガイド / 出側設備 / 装入設備）を
個別に選べます。既定は装入設備を除く 3 つ。板に接するローラ類（ワークロール・
サイドガイドローラ・テーブルローラ）は «当たっていることが見えるべき部材» なので
透過対象から外しています。

`PhysicsEngine.update(dt)` は内部で 1/120 s の固定ステップに分割するため、
**12 fps でも 144 fps でも結果が一致します**（`tools/eval.mjs` が 12 / 30 / 60 / 144 fps を
走らせて最終板長とパス数の完全一致を毎回確かめています）。シミュレーション速度スライダは
入力 dt を変えるだけで、
物理結果には影響しません。

---

## 4. 描画性能

| 指標 | 値 |
|---|---|
| ドローコール | 70–85（カメラ位置による） |
| 描画三角形 | 約 50,000（シーン全体 64,811） |
| ユニークジオメトリ | 63 |
| シェーダプログラム | 14 |
| InstancedMesh | 32（インスタンス総数 546） |

繰り返し部材（テーブルローラ約 160 本・架台・ガイドローラ・チョック等）は `InstancedMesh` に集約し、
テーブルは区間分割して視界外をフラスタムカリングで落としています。静的部材は `mergeGeometries` で
1 メッシュに畳んでいます。

環境反射（PMREM）はフラグメント単価が高いため、**磨かれた金属部材にのみ**適用し、画面占有面積の
大きい塗装構造物・床には適用していません。描画品質は「高 / 標準 / 軽量」で切り替えられ、
起動直後にフレームレートを実測して 20 fps を下回る環境では自動的に軽量へ切り替わります。

---

## 5. 自動検査

**直す前に測り、直したあとも同じ測り方で確かめる。**その測り方は評価器として残します
（一度見つけた不具合が二度と戻らないようにするため）。**59 本すべてが通る状態**を保っています。

評価器はすべて `tools/harness.mjs` の同じ起動経路（three.js のローカル配信、`App` の露出、
固定ステップの早送り、カメラ設定）を使い、計測条件の食い違いを構造的に無くしています。

```bash
node tools/smoke.mjs          # 起動して 1 ロット通す（いちばん軽い確認）
node tools/<name>.mjs         # 個別に走らせる。末尾に RESULT: PASS (n/n) が出る

# 全部通す（4 並列。1 本あたり 1〜5 分、全体で 40〜50 分）
for f in tools/*.mjs; do t=$(basename $f .mjs); [ "$t" = harness ] && continue
  node $f > /tmp/$t.log 2>&1; echo "$t $? $(grep -E 'RESULT' /tmp/$t.log | tail -1)"; done
```

**判定に数えるのは «実測の裏づけがあるもの» だけ**です。しきい値の根拠が無い項目は
`??` を付けて**参考**として毎回数値だけを出します（`RESULT: PASS (n/n、参考 m 件)`）。

### 圧延モデル（物理）

| 評価器 | 見るもの |
|---|---|
| `modeltrace.mjs` | 圧延モデルが**物理として正しいか**。結果の数値ではなく «式が持つべき性質» を問う —— 構成式の恒等式、Q と n を式から逆算して表の値と一致するか、m_eff が数値微分と一致するか、扁平・前進率・ミルばね・ワニ口 |
| `precisetrace.mjs` | 構成式・制御・熱の**検算**。実装が使う式とは**別に解き直した値**と突き合わせる |
| `audit.mjs` | 物理実装の**独立検証**。極限・保存則で確かめる |
| `sensload.mjs` | 圧延荷重に **«何が・どれだけ» 効くか**。感度（∂lnF/∂lnX）と、それに実機の振れ幅を掛けた影響度。`docs/圧延荷重に効く要因.md` の数字はすべてここから出ます |
| `mtrace.mjs` | ひずみ速度感受性 m の**同定**と、速度を変えたときの荷重変化の**分解**（ひずみ速度／摩擦）。荷重とトルクの感度行列 |
| `microtrace.mjs` | 組織の履歴 —— 亜結晶粒径・静的再結晶率・残留ひずみ |
| `voltrace.mjs` | **体積が保たれるか**を素材長を変えて。保存則は «板 ＋ 切り落とした材料 ＝ 素材»（`--only=3450` で 1 つだけ速く回せます） |
| `calib.mjs` | **実機データとの較正**。実機のパススケジュールをそのまま流し、荷重帯・上がり温度・板長を突き合わせる |
| `realsched.mjs` / `reallot.mjs` | 実機のパススケジュールを流したパス表 ／ 較正の基準データ |
| `headtail.mjs` | **頭・尻の荷重差の要因分解**（入側板厚・局所温度・速度・実ギャップ） |
| `loadtrace.mjs` | **1 パスの中での荷重の形**。過負荷停止せず通せるか、噛み込みのピーク、端部、**板厚制御が発振しないか**（制御帯域の往復とスタンドの固有振動を分けて出す） |
| `schedtrace.mjs` | **パススケジュール生成**を材質 × 呼び厚 × 板幅 × 板長の総当たりで |
| `gaugetrace.mjs` | **長手方向の板厚変動と X 線板厚計**。振れの大きさ・偏芯波長の自己相関・外乱の内訳（偏芯／摩擦の揺らぎ／スタンドの鳴きを 1 つずつ切って測る）・計器の据付と読み |
| `shapetrace.mjs` / `shaperun.mjs` | **幅方向の形状**（クラウン・平坦度・エッジドロップ） ／ 通しで走らせたパス表 |
| `curltrace.mjs` / `curlfixtrace.mjs` / `curlmeas.mjs` | パスごとの層温度・反り曲率・浮き上がり ／ **板反り対策の効き** ／ 反りが弾性で戻る量との比 |
| `cambertrace.mjs` | **蛇行（キャンバーと横行）**、差荷重制御 |
| `edgetrace.mjs` | **鋳肌ラウンド断面**（面削面が平ら・コーナーが自己相似に縮む） |
| `thermcheck.mjs` / `cooltrace.mjs` | 層熱モデルの単体検証 ／ **冷却モデル**（上面のプール・下面の直撃スプレー・沸騰曲線） |
| `soaktrace.mjs` | **ソーキングマスタ**（実機の操業基準 39 パターン）の整合。参照切れ・段数と均熱段の噛み合い・«判定温度» と «設定温度» の区別・代表材からの引き当て |
| `coolfit.mjs` | **上がり温度の合わせ込み**。冷却の組み合わせと液流束を振って、上がり温度・板長・上下差・先端の浮きを並べて出す |
| `curlbal.mjs` | **上下の冷却バランスが反りをどれだけ動かすか**。入側の «上面だけ» と «上下とも» を分けて比べる（«入れても反らない» の正体がここ） |
| `brushtrace.mjs` | **ロールコーティングとブラシロール** |

### 主駆動系

| 評価器 | 見るもの |
|---|---|
| `drivetrace.mjs` | **トルク収支**が «圧延 ＋ 軸受 ＋ BUR を連れ回す ＋ 慣性» の 4 項で閉じるか |
| `ecctrace.mjs` | **偏芯**。«停止時間の抜熱ムラ» から出る熱偏芯（止めるほど育ち、回すと均される）と、荷重の回転同期成分からの補償（同期検波・応答遅れの先回り・ゲージメータ AGC との干渉） |
| `screwtrace.mjs` | **圧下装置**（電動圧下 2 速・油圧圧下）。仕様の検算、油圧の周波数応答（5 Hz で 90° 遅れ・±50 µm）、アクチュエータごとの速度の枠、荷重の 2 段リミット（1st 荷重一定制御・2nd 急速開） |
| `reversetrace.mjs` | **逆転**（制動・加速・トルク・BUR のすべり上限） |
| `speedtrace.mjs` | 板の **«見た目の速度»**。同じ時間を物理と描画の両方へ通して比べる |

### 設備・構造・配置

| 評価器 | 見るもの |
|---|---|
| `structure.mjs` | 静的部材の**接地**（床・ピット床・支持部材への連鎖）、開口上の浮き、部材どうしの食い込み |
| `interfere.mjs` | 圧延サイクル中の**板と設備の意図しない接触**（三角形 AABB） |
| `layouttrace.mjs` | テーブルローラの本数とピッチ（図面どおりか）、計器とサイドガイドの取り合い、群配置 |
| `rolltrace.mjs` | **ロール組立て（チョック付き）**を圧下の全域で |
| `rollshoptrace.mjs` | **ロール現品管理**（保有組数・寸法・研削クラウン） |
| `supplytrace.mjs` / `pittrace.mjs` / `pitplan.mjs` | 装入工程で回る板と設備の食い込み ／ 装入まわりの配置とピットトング ／ **ピット炉の仕様とスラブの置き方（A/B/C）** |
| `tongtrace.mjs` / `cranetrace.mjs` | 可動部と他設備の食い込み ／ **ピットクレーンの移行速度** |
| `croptrace.mjs` / `dividetrace.mjs` | 75 mm シャーの下台・端材・板の実座標 ／ **板材の切り分けとパイラー積み** |
| `scraptrace.mjs` / `dragtrace.mjs` | **クロップ屑の払い出し**（受け → 傾斜コンベア → 屑箱） ／ **屑の手掴み**（実際のポインタ操作を送る） |
| `coiltrace.mjs` / `wraptrace.mjs` / `holdtrace.mjs` / `cartrace.mjs` | 巻き数と巻き長さ ／ **ベルトラッパー** ／ **コイル押えの当て方** ／ **コイルカーの足まわり** |
| `nozzletrace.mjs` / `steamtrace.mjs` | 板面冷却ノズルの**着地点** ／ **着水蒸気の発生点** |
| `labeltrace.mjs` | **設備ラベルが実物の上を指しているか** |

### 画面・記録

| 評価器 | 見るもの |
|---|---|
| `eval.mjs` | 描画コスト・回転方向・固定 dt での物理（体積保存・荷重・温度・dt 非依存）・幾何/構造の全数検査・版数バッジと `VERSION` の一致 |
| `uicheck.mjs` | **UI が崩れないこと**を 4 つの画面幅（1920 / 1728 / 1536 / 1280）で |
| `logtrace.mjs` | **圧延実績（ロギング）が実際に起きたことと合っているか**、実績ビュワーの操作 |
| `plantrace.mjs` | **運転計画と書き出し**（押したとおりに状態が変わるか） |
| `smoke.mjs` | 自動運転の完走と要約 |
| `shots.mjs` / `uishot.mjs` / `geoshot.mjs` / `carshot.mjs` / `yardshot.mjs` / `vis.mjs` | 定点撮影（判定はせず、目で見るための絵を出す） |

### 意図しない接触の全数検査（`tools/interfere.mjs`）

「板がどこかに当たっていないか」は目視では追いきれないので、**評価器**を用意しました。

1. 圧延サイクルを回しながら 0.5 秒ごとにサンプリングする
2. その時刻の板の占有ボックス（板厚・板幅・先尾端）を作る
3. シーン中の全メッシュを**三角形単位の AABB** に落とし、板ボックスと重なるものを拾う
4. **当たってよい部材**（ワークロール・テーブルローラ・サイドガイドローラ・トリマー刃・
   スリーロール・シャー刃・マンドレル・押えロール等）を除外し、残りを干渉として報告する

この検査で見つかり、修正した実例：

| 部材 | 症状 | 原因と対策 |
|---|---|---|
| クーラントヘッダ | 382 mm スラブに 49 mm 食い込む | ロール表面からの距離で置いていたため、圧下量が大きいとパスライン内に入っていた。**ロール軸心からの距離**（内側へ 220 mm）で置き直した |
| トリマー刃物軸 | 通し軸が板の上を横切る | OS/DS 別の**片持ち軸**に分離。刃位置に合わせて張り出し量が変わる |
| トリマー上刃 | 厚板パスで板に当たる | 図面の開位置 ±305 mm は薄板時のもの。**板厚 + 刃半径 + 30 mm** まで自動退避させる |
| トリマー上下刃 | 同じ Z に置かれ刃どうしが食い込む | せん断面を挟んで**軸方向に隣り合わせ**（上刃＝スクラップ側、下刃＝製品側） |
| 巻取部のラッパーロール | 巻取中にコイルと干渉 | 削除し、**トリマー側からアームで伸びる押えロール**（面長 600 mm）に置き換え |

この検査は 75 mm シャーの作り直しでも働きました。エプロンの脚・側レール・払い出しプッシャが
それぞれ «刃の通り道» や «板の通る範囲» に入り込んでいたのを検出し、位置を直しています。

現在の検査結果は **干渉 0 件**です。

### 静的構造の全数検査（`tools/structure.mjs`）

«ピットの上に架台が立っている» «基礎が床下に潜っている» «梁が宙に浮いている» は目視で
見落としやすいので、可動部を除くすべてのメッシュについて機械的に判定します。

1. **接地**: 底面が床（y = 0）かピット床（y = −3,600・ピット内）に着いている部材を接地とし、
   接地部材に 10 mm 以内で接している部材を連鎖的に «支持されている» とする。床版を貫く部材
   （ピット外で底面が床下）は不成立
2. **開口上の浮き**: 床面高さの底面が圧延機ピット・ピット炉の開口の真上に無いか
3. **食い込み**: 別メッシュどうしの三角形 AABB が 20 mm を超えて重なっていないか
4. **体積の食い込み（レイキャスト）**: 3 は軸に平行な面どうし（箱と箱）の重なりを拾えない
   （平らな三角形はその軸方向の厚みが 0 で、重なり量が 0 になる）。そこで部材 A の頂点・重心が
   部材 B の中にあるかをレイの交差回数（偶奇）で判定し、中にある点から 6 軸方向の最近面までの
   距離を食い込み深さとする。今回これを加えたところ、以前の 0 件の裏に **32 件**（トリマー基礎と
   3ロール基礎、巻取機の駆動側架台とテーブル架台、ハウジングを貫くタイビーム、細ピッチ区間で
   隣どうしが重なる架台、新設のシャー・板面クーラント架台の柱など）があり、すべて位置・寸法を
   直して 0 件にした

現在は接地不成立・開口上・食い込み（三角形 AABB・レイキャストとも）**すべて 0 件**です。装入工程は
`tools/supplytrace.mjs` が、回転するスラブと装入設備（転倒機・クレーン・走行路）の三角形を分離軸判定で
照合し、**食い込み 0 件**です。

---

## 6. 操作

| キー | 動作 |
|---|---|
| `Space` | 圧延サイクル開始 / 中止 |
| `P` | 一時停止 / 再開 |
| `R` | リセット |
| `L` | 設備ラベル表示切替 |
| `C` | 断面表示切替（実体 / 半断面 / 透視） |
| `1`–`9`, `0` | 視点切替（全景 / 圧延機 / ロールバイト / 駆動側 / 装入設備 / 巻取設備 / 75mmシャー / 30mmシャー・パイラー / 板面冷却 / ピット炉・スラブヤード） |
| `M` | メニュー（サイドバー）を畳む / 開く |

マウス：左ドラッグ回転 / 右ドラッグ平行移動 / ホイール拡大縮小

サイドバーは **ブランド → 主操作（開始/中止・一時停止・リセット）→ 4 つのタブ（準備／運転／表示／設備）→
タブの中身** の固定構造です。準備タブは作業の流れ順（① 素材と目標 → ② パススケジュール）、運転タブは
運転モードと板反り対策、表示タブは断面・描画品質・凡例・演出・ショートカット、設備タブは諸元とロール現品・
テーブル区分です。タブの下段に要約（材質と目標厚／モードと倍率／断面と品質／ロール状態）が出るので、
開かなくても今の値が読めます。**主操作とタブの位置は、状態やメッセージの長さ、タブの切替で動きません。**
「今どうなっていて、次に何をするか」の状態文と一時通知は、サイドバーではなく**計器バーの真上の状態行**
（高さ固定・1 行）に出ます。運転中に頻繁に使う視点・断面・注記・実績は画面上部のツールバー 1 行にあり、
視点は短い名前とキー番号の帯（`1`〜`9`・`0`）です。

素材パネルでは **材質（7 合金）・呼び厚・面削量・幅・長さ・炉出し温度・目標板厚（4〜150 mm）・
トリム量** を設定します。待機中の変更は即座にパススケジュールへ反映され、仕上げ形態
（コイル / 板材）と使う設備（75 mm シャーの要否）が併記されます。運転中の変更は
「適用してリセット」で反映します。計器バーには材料温度の上/芯/下と反り半径（端の浮き・
上下温度差）を出しています。
